name: Deploy Pulse

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Deployment environment"
        required: true
        type: choice
        options:
          - dev
          - prod
  push:
    branches: [main]
    paths:
      - "bowrain/apps/pulse/**"
      - "bowrain/packages/ui/**"

jobs:
  build:
    name: Build Pulse Assets
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup-node-ui

      - name: Build pulse app
        working-directory: bowrain/apps/pulse
        run: vp run build

      - name: Upload pulse artifacts
        uses: actions/upload-artifact@v7
        with:
          name: pulse-dist
          path: bowrain/apps/pulse/dist
          retention-days: 7

  trigger-deploy:
    name: Trigger Deploy
    needs: build
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.NEOKAPI_GITHUB_TOKEN }}
      ENVIRONMENT: ${{ github.event.inputs.environment || 'dev' }}
    steps:
      - name: Deploy pulse web assets
        env:
          RUN_ID: ${{ github.run_id }}
        run: |
          gh workflow run deploy-pulse-web.yml \
            --repo neokapi/bowrain-infra \
            -f environment="$ENVIRONMENT" \
            -f runId="$RUN_ID"
