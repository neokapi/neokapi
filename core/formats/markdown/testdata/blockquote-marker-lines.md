# Blockquote marker lines

A blockquote line that carries the marker and nothing else keeps the space
after its last angle bracket.

> 
> A quote that opens after an empty marker line.
> 
> A second paragraph inside the same quote.

>> 
>> A nested quote after an empty marker line.
