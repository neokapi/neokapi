format: odf
# Native ODF reads/writes are now canonical-equal with the upstream
# okapi reference on all 15 fixtures. Bridge is byte-equal on all 15
# (literally invokes Okapi's OpenOfficeFilter via okapi-bridge#11's
# two-pass pipeline). No outstanding native divergences.
fixtures: {}
