{
  "generated": "2026-08-28",
  "repo": "lab-repo/ripgrep-14.1.1",
  "guides": {
    "end-user": "# Voice Guide: ripgrep\n\nThe voice of ripgrep's own documentation, inferred by kapi from README.md, GUIDE.md and FAQ.md at 14.1.1, then corrected by hand.\n\n## Tone\n- Personality: knowledgeable peer, not a vendor, patient explainer who shows the reasoning, not just the answer, candid about limits and unknowns, pragmatic: gives the workaround even when the cause is unfixed, generous toward other tools and their users\n- Formality: technical\n- Emotion: neutral\n- Humor: light\n- Guidelines: Calm and matter-of-fact. Enthusiasm is reserved for genuinely good news (\"Yes!\") and mild exasperation is permitted at real problems (\"Owch.\"). Answer the literal question first, in one word or one sentence when possible (\"Yes. See the guide's section on configuration files.\"), then expand. Explain mechanism when it changes what the reader should do. Say plainly what you do not know rather than bluffing. Never promise dates or outcomes you cannot keep; state the policy and the reason instead. Treat other tools as legitimate choices; explain ripgrep's design on its own rationale. Humor is dry, rare, and arises from the situation, never from a joke inserted for effect.\n\n## Style Rules\n- Use active voice\n- Sentence length: short\n- Point of view: second\n- Contractions: always\n\n## Vocabulary\n### Preferred Terms\n- **ripgrep**: Always lowercase, including at the start of a sentence: \"ripgrep is a project whose contributors are volunteers.\"\n- **rg**: The binary name in commands and transcripts; prose says \"ripgrep\", shell lines say `rg`.\n- **flag**: Dominant word for CLI arguments: \"the -U/--multiline flag\", \"two flags related to colors\". \"options\" appears only when referring to `rg --help` output.\n- **-P/--pcre2**: Cite short and long forms together as a slash pair on first mention: -f/--file, -z/--search-zip, -U/--multiline.\n- **Note that**: Standard caveat opener, also used parenthetically as \"(Note: ...)\".\n- **regex engine**: Used consistently for both the default and PCRE2; \"default regex engine\" contrasts with \"PCRE2\".\n- **true color**: Two words, lowercase, for RGB support: \"true color support already works out of the box\".\n- **work around**: Verb is two words (\"you can work around by simply increasing the limit\"); noun is hyphenated (\"The work-around above\").\n\n### Forbidden Terms\n- ~~Ripgrep~~ → use **ripgrep**: Capitalized forms (Ripgrep, RipGrep, RIPGREP) never appear; the name is lowercase in every one of ~60 occurrences.\n- ~~blazing fast~~ → use **a timed comparison, e.g. \"real 0m1.783s\" vs \"real 0m2.458s\"**: Performance is always demonstrated with `time` output or `--trace` lines, never asserted.\n- ~~we guarantee / we promise~~ → use **\"generally\", \"on a best effort basis\", \"no promises are made\"**: The release-policy entry hedges deliberately: \"However, no promises are made.\"\n- ~~utilize~~ → use **use**: \"use\" throughout: \"ripgrep uses parallelism\", \"Use the --colors flag\".\n- ~~crate~~ → use **part of ripgrep**\n- ~~struct~~ → use **leave it out**\n- ~~trait~~ → use **leave it out**\n- ~~API~~ → use **leave it out**\n- ~~implementation~~ → use **how it works**\n- ~~instantiate~~ → use **create**\n- ~~glob matcher~~ → use **file name pattern**\n\n### Competitor Terms\n- ~~ag~~ → use **The Silver Searcher**: Referred to by full name with the article, and accommodated rather than attacked: an entire entry explains how to emulate its output style.\n- **grep**: Named plainly and given its own entry (\"Can ripgrep replace grep?\"); no put-downs.\n- **PCRE2**: A supported alternative engine. Tradeoffs are stated factually (\"Both regex engines are quite fast\") and unknowns are conceded: \"Why doesn't PCRE2 do the same? This author isn't familiar with its internals.\"\n- **cygwin**: Lowercase, described as a source of path-mangling behavior with three concrete fixes offered rather than a verdict.\n\n## Examples\n### Example 1\n- Before: \"RipGrep is a blazingly fast, next-generation search tool that leaves The Silver Searcher in the dust.\"\n- After: \"By default, ripgrep uses parallelism to execute its search because this makes the search much faster on most modern systems.\"\n- Why: Lowercase product name, no superlatives, no competitor put-down, and the speed claim is tied to a stated mechanism (parallelism) rather than asserted as branding.\n\n### Example 2\n- Before: \"The next release is currently scheduled for Q3, though this may slip.\"\n- After: \"ripgrep is a project whose contributors are volunteers. A release schedule adds undue stress to said volunteers. Therefore, releases are made on a best effort basis and no dates will ever be given.\"\n- Why: States the policy and the reason behind it instead of a date. The corpus is explicit that no dates are given, and immediately follows with the honest exception for high-impact bugs.\n\n### Example 3\n- Before: \"It should be noted that users may configure the man page location, and the documentation should then be accessible via standard mechanisms.\"\n- After: \"Note that the man page's documentation for options is equivalent to the output shown in rg --help. To see more condensed documentation (one line per flag), run rg -h.\"\n- Why: Bare \"Note that\", concrete commands instead of abstract \"standard mechanisms\", and second person addressed through an imperative. Every how-to answer in the corpus lands on a runnable `$` line.\n\n### Example 4\n- Before: \"Unfortunately, we are unable to provide details on PCRE2's internal behavior at this time.\"\n- After: \"Why doesn't PCRE2 do the same? This author isn't familiar with its internals, so we can't comment on it here.\"\n- Why: Candid admission of a limit, in contractions, without corporate hedging or apology. The rhetorical question is the corpus's habitual device for advancing a technical explanation.\n\n"
  },
  "tasks": {
    "end-user": "Read this repository and write a user guide, in Markdown, of roughly 800 words, for ripgrep's file-type filtering: what `--type`, `--type-not`, `--type-add` and `--type-list` do and how someone uses them. The reader searches text for a living. Ground every claim in what the source and the existing docs actually say."
  },
  "labels": {
    "end-user": "End user (non-technical)"
  },
  "profile": "name: ripgrep\ndescription: The voice of ripgrep's own documentation, inferred by kapi from README.md,\n    GUIDE.md and FAQ.md at 14.1.1, then corrected by hand.\ntone:\n    personality:\n        - knowledgeable peer, not a vendor\n        - patient explainer who shows the reasoning, not just the answer\n        - candid about limits and unknowns\n        - 'pragmatic: gives the workaround even when the cause is unfixed'\n        - generous toward other tools and their users\n    formality: technical\n    emotion: neutral\n    humor: light\n    guidelines: Calm and matter-of-fact. Enthusiasm is reserved for genuinely good news\n        (\"Yes!\") and mild exasperation is permitted at real problems (\"Owch.\"). Answer the literal question first, in one word or one sentence when possible (\"Yes. See the guide's section on configuration files.\"), then expand. Explain mechanism when it changes what the reader should do. Say plainly what you do not know rather than bluffing. Never promise dates or outcomes you cannot keep; state the policy and the reason instead. Treat other tools as legitimate choices; explain ripgrep's design on its own rationale. Humor is dry, rare, and arises from the situation, never from a joke inserted for effect.\nstyle:\n    active_voice: true\n    sentence_length: varied\n    person_pov: second\n    contractions: always\n    prohibited_patterns:\n        - regex: '\\x{2014}'\n          description: Em dashes. The corpus contains none across ~25 entries; asides are carried by parentheses, semicolons, or a new sentence.\n          severity: minor\n        - regex: (?i)\\b(blazing(ly)?[ -]fast|lightning[ -]fast|revolutionary|game[ -]?chang(er|ing)|seamless(ly)?|effortless(ly)?|best[ -]in[ -]class|supercharge[ds]?)\\b\n          description: Marketing superlatives. Speed claims in the corpus are always concrete and measured (`real 0m1.783s` vs `real 0m2.458s`), never adjectival.\n          severity: major\n        - regex: (?i)\\b(bloated|inferior|obsolete|crappy|garbage|clunky|no reason to use)\\b\n          description: Disparaging other tools. grep, The Silver Searcher and PCRE2 are all discussed neutrally or accommodatingly.\n          severity: major\n        - regex: (?i)(scheduled for (Q[1-4]|\\w+ \\d{4})|will be released (on|in|by)|ETA\\b|coming soon)\n          description: 'Release-date promises. The corpus states the opposite policy explicitly: \"releases are made on a best effort basis and no dates will ever be given.\"'\n          severity: major\n        - regex: (?i)\\b(it should be noted that|please be aware that|please note that)\\b\n          description: 'Padded caveat openers. The corpus uses bare \"Note that ...\" or a parenthetical \"(Note: ...)\".'\n          severity: minor\n        - regex: (?i)\\b(utilize|leverage|facilitate)\\b\n          description: Latinate substitutes for plain verbs. The corpus uses \"use\", \"uses\", \"lets you\" without exception.\n          severity: minor\n        - regex: '[\\x{1F300}-\\x{1FAFF}\\x{2600}-\\x{27BF}\\x{FE0F}]'\n          description: Emoji and decorative symbols. Absent from the entire corpus, including headings and warnings.\n          severity: minor\nvocabulary:\n    preferred_terms:\n        - term: ripgrep\n          replacement: ripgrep\n          note: 'Always lowercase, including at the start of a sentence: \"ripgrep is a project whose contributors are volunteers.\"'\n        - term: rg\n          replacement: rg\n          note: The binary name in commands and transcripts; prose says \"ripgrep\", shell lines say `rg`.\n        - term: flag\n          replacement: flag\n          note: 'Dominant word for CLI arguments: \"the -U/--multiline flag\", \"two flags related to colors\". \"options\" appears only when referring to `rg --help` output.'\n        - term: -P/--pcre2\n          replacement: -P/--pcre2\n          note: 'Cite short and long forms together as a slash pair on first mention: -f/--file, -z/--search-zip, -U/--multiline.'\n        - term: Note that\n          replacement: Note that\n          note: 'Standard caveat opener, also used parenthetically as \"(Note: ...)\".'\n        - term: regex engine\n          replacement: regex engine\n          note: Used consistently for both the default and PCRE2; \"default regex engine\" contrasts with \"PCRE2\".\n        - term: true color\n          replacement: true color\n          note: 'Two words, lowercase, for RGB support: \"true color support already works out of the box\".'\n        - term: work around\n          replacement: work around\n          note: Verb is two words (\"you can work around by simply increasing the limit\"); noun is hyphenated (\"The work-around above\").\n    forbidden_terms:\n        - term: Ripgrep\n          replacement: ripgrep\n          note: Capitalized forms (Ripgrep, RipGrep, RIPGREP) never appear; the name is lowercase in every one of ~60 occurrences.\n        - term: blazing fast\n          replacement: a timed comparison, e.g. \"real 0m1.783s\" vs \"real 0m2.458s\"\n          note: Performance is always demonstrated with `time` output or `--trace` lines, never asserted.\n        - term: we guarantee / we promise\n          replacement: '\"generally\", \"on a best effort basis\", \"no promises are made\"'\n          note: 'The release-policy entry hedges deliberately: \"However, no promises are made.\"'\n        - term: utilize\n          replacement: use\n          note: '\"use\" throughout: \"ripgrep uses parallelism\", \"Use the --colors flag\".'\n    competitor_terms:\n        - term: ag\n          replacement: The Silver Searcher\n          note: 'Referred to by full name with the article, and accommodated rather than attacked: an entire entry explains how to emulate its output style.'\n        - term: grep\n          replacement: grep\n          note: Named plainly and given its own entry (\"Can ripgrep replace grep?\"); no put-downs.\n        - term: PCRE2\n          replacement: PCRE2\n          note: 'A supported alternative engine. Tradeoffs are stated factually (\"Both regex engines are quite fast\") and unknowns are conceded: \"Why doesn''t PCRE2 do the same? This author isn''t familiar with its internals.\"'\n        - term: cygwin\n          replacement: cygwin\n          note: Lowercase, described as a source of path-mangling behavior with three concrete fixes offered rather than a verdict.\nexamples:\n    - before: RipGrep is a blazingly fast, next-generation search tool that leaves The Silver Searcher in the dust.\n      after: By default, ripgrep uses parallelism to execute its search because this makes the search much faster on most modern systems.\n      explanation: Lowercase product name, no superlatives, no competitor put-down, and the speed claim is tied to a stated mechanism (parallelism) rather than asserted as branding.\n    - before: The next release is currently scheduled for Q3, though this may slip.\n      after: ripgrep is a project whose contributors are volunteers. A release schedule adds undue stress to said volunteers. Therefore, releases are made on a best effort basis and no dates will ever be given.\n      explanation: States the policy and the reason behind it instead of a date. The corpus is explicit that no dates are given, and immediately follows with the honest exception for high-impact bugs.\n    - before: It should be noted that users may configure the man page location, and the documentation should then be accessible via standard mechanisms.\n      after: Note that the man page's documentation for options is equivalent to the output shown in rg --help. To see more condensed documentation (one line per flag), run rg -h.\n      explanation: Bare \"Note that\", concrete commands instead of abstract \"standard mechanisms\", and second person addressed through an imperative. Every how-to answer in the corpus lands on a runnable `$` line.\n    - before: Unfortunately, we are unable to provide details on PCRE2's internal behavior at this time.\n      after: Why doesn't PCRE2 do the same? This author isn't familiar with its internals, so we can't comment on it here.\n      explanation: Candid admission of a limit, in contractions, without corporate hedging or apology. The rhetorical question is the corpus's habitual device for advancing a technical explanation.\n\n# Two points in the context space, expressed as the profile's own personas.\n#\n# Hand-added, not inferred: ripgrep's docs do not separate these audiences, so\n# there was nothing for inference to find. This is the part a person writes.\n#\n# core/profile.ResolveProfile applies a persona's vocabulary as a TIGHTENING\n# only: avoided_terms extend the forbidden set, and a preferred term the profile\n# already forbids is dropped. So neither reader can re-allow what ripgrep's\n# voice bans.\npersonas:\n    end-user:\n        style:\n            active_voice: true\n            person_pov: second\n            contractions: always\n            sentence_length: short\n        avoided_terms:\n            - term: crate\n              replacement: part of ripgrep\n              severity: major\n              forms: [crates]\n            - term: struct\n              replacement: leave it out\n              severity: major\n              forms: [structs]\n            - term: trait\n              replacement: leave it out\n              severity: major\n              forms: [traits]\n            - term: API\n              replacement: leave it out\n              severity: major\n            - term: implementation\n              replacement: how it works\n              severity: minor\n            - term: instantiate\n              replacement: create\n              severity: major\n              forms: [instantiates, instantiated, instantiating]\n            - term: glob matcher\n              replacement: file name pattern\n              severity: minor\n    developer:\n        style:\n            active_voice: true\n            person_pov: second\n            contractions: always\n            sentence_length: varied\n        avoided_terms:\n            - term: simply\n              severity: major\n            - term: just\n              replacement: say what it takes instead\n              severity: major\n            - term: easy\n              replacement: say what it takes instead\n              severity: major\n              forms: [easily]\n            - term: straightforward\n              severity: major\n            - term: under the hood\n              replacement: name the module\n              severity: minor\n",
  "runner": "claude -p in the cloned tree, 60 turns maximum per document, tools on, isolation contract applied. Documents are the output; nothing is scored.",
  "docs": [
    {
      "model": "claude-haiku-4-5",
      "audience": "end-user",
      "bare": {
        "text": "# File Type Filtering in ripgrep\n\nSearching your codebase is often about finding patterns in specific kinds of files. You might want to search only Python files, or exclude all the vendored JavaScript, or limit your results to configuration files. This is where ripgrep's file type filtering comes in. Instead of writing out glob patterns every time, you can use simple, memorable type names that ripgrep knows about.\n\n## What are file types?\n\nA file type in ripgrep is a name that represents one or more file patterns (globs). For example, the `rust` type corresponds to all files matching `*.rs`. The `c` type matches both C source files (`*.c`) and header files (`*.h` and `*.H`). Ripgrep ships with over 200 predefined types covering the major programming languages and file formats in common use.\n\nFile types are just a convenience layer on top of glob patterns. When you use `rg 'pattern' --type rust`, it's equivalent to `rg 'pattern' -g '*.rs'`. The type approach saves you from having to remember which extensions belong to each language or format.\n\n## Selecting files with `--type`\n\nThe `--type` flag (short form: `-t`) tells ripgrep to search only files of a specific type. For example, to search for function definitions only in Rust files:\n\n```bash\nrg 'fn run' --type rust\n```\n\nOr more concisely:\n\n```bash\nrg 'fn run' -trus\n```\n\nYou can use multiple `--type` flags to include multiple file types in your search:\n\n```bash\nrg 'TODO' --type rust --type go --type python\n```\n\nThis searches for \"TODO\" comments across three programming languages at once.\n\n## Excluding files with `--type-not`\n\nSometimes it's easier to say what you don't want to search. The `--type-not` flag (short form: `-T`) excludes files of a specified type:\n\n```bash\nrg 'console.log' --type-not javascript\n```\n\nThis searches for \"console.log\" everywhere except JavaScript files. Like `--type`, you can use `--type-not` multiple times:\n\n```bash\nrg 'import' --type-not python --type-not go --type-not rust\n```\n\nThis searches for \"import\" in any file type except Python, Go, and Rust.\n\nThe distinction between `--type` and `--type-not` matters for whitelist vs. blacklist behavior. When you use `--type`, ripgrep searches only files matching those types. When you use `--type-not`, ripgrep searches all files except those types, including files that don't match any known type. This is useful when you're searching text that might appear in miscellaneous files ripgrep doesn't have a specific type for.\n\n## Discovering available types with `--type-list`\n\nTo see what types ripgrep knows about, use the `--type-list` flag. This prints every available type and its corresponding globs:\n\n```bash\nrg --type-list\n```\n\nThe output looks like this:\n\n```\nada: *.adb, *.ads\nagda: *.agda, *.lagda\nasm: *.asm, *.s, *.S\nc: *.[chH], *.[chH].in, *.cats\ncpp: *.[ChH], *.cc, *.[ch]pp, *.[ch]xx, *.hh, *.inl, *.[ChH].in, *.cc.in, *.[ch]pp.in, *.[ch]xx.in, *.hh.in\ngo: *.go\npython: *.py\nrust: *.rs\n```\n\nEach type is shown on its own line, followed by a colon and a comma-delimited list of globs. If you need to remind yourself which extensions belong to a language you don't work in often, `--type-list` has you covered.\n\nYou can pipe `--type-list` output to ripgrep itself to find types matching a pattern:\n\n```bash\nrg --type-list | rg '^make:'\n```\n\nThis shows you exactly what patterns the `make` type includes.\n\n## Creating custom types with `--type-add`\n\nThe built-in types cover most languages and formats, but sometimes you need something custom. The `--type-add` flag lets you define new types for your search:\n\n```bash\nrg 'title' --type-add 'web:*.html' --type-add 'web:*.css' --type-add 'web:*.js' --type web\n```\n\nThis defines a custom `web` type that includes HTML, CSS, and JavaScript files, then searches only those files for \"title\". You can make this more concise using the glob braces syntax:\n\n```bash\nrg 'title' --type-add 'web:*.{html,css,js}' --type web\n```\n\n### Important: types defined with `--type-add` are temporary\n\nThe types you create with `--type-add` exist only for that one ripgrep invocation. If you exit your shell and run the command again later, ripgrep won't know about your custom type anymore. This is by design—types are not persisted to disk.\n\nIf you want a custom type to be available every time you run ripgrep, add it to your ripgrep configuration file. Create or edit `~/.ripgreprc` (or set `$RIPGREP_CONFIG_PATH` to point to a different location) and add lines like:\n\n```\n--type-add=web:*.{html,css,js}\n--type-add=data:*.json\n--type-add=data:*.yaml\n```\n\nNow you can use `rg 'pattern' --type web` without having to specify `--type-add` on the command line each time.\n\n## The special `all` type\n\nRipgrep recognizes a special type called `all` that refers to every file type ripgrep knows about. Using `--type all` is equivalent to running `--type rust --type python --type go` and so on for every single built-in type plus any custom types you've added:\n\n```bash\nrg 'pattern' --type all\n```\n\nThis is rarely what you want for a search—you're usually looking at a specific subset of files. But `--type-not all` has a specific and useful purpose: it searches only files that don't match any known file type. These might be configuration files without standard extensions, documentation files, or other miscellaneous text:\n\n```bash\nrg 'pattern' --type-not all\n```\n\nThis searches in \"blacklist\" mode, finding your pattern in any file that ripgrep's type system doesn't recognize.\n\n## Practical examples for daily work\n\nHere are some common scenarios for people who search code for a living:\n\n**Search for a symbol only in your primary language:**\n\n```bash\nrg 'User\\(' --type rust\n```\n\n**Look for TODO comments everywhere except in third-party libraries:**\n\n```bash\nrg 'TODO|FIXME' --type-not javascript --type-not node_modules\n```\n\n**Audit all shell scripts in a monorepo:**\n\n```bash\nrg 'eval\\(' --type sh\n```\n\n**Search for hardcoded API endpoints in frontend code:**\n\n```bash\nrg 'api\\.example\\.com' --type-add 'frontend:*.tsx' --type-add 'frontend:*.jsx' --type-add 'frontend:*.ts' --type frontend\n```\n\n**Find all imports in a polyglot repository, but exclude generated code:**\n\n```bash\nrg '^(import|require|from)' --type rust --type python --type go\n```\n\n## Performance and filtering precedence\n\nFile type filtering works with ripgrep's other filtering mechanisms. By default, ripgrep still respects `.gitignore`, `.ignore`, and `.rgignore` files, skips binary files, and skips hidden files. Type filtering adds an additional layer on top of these—it narrows your search space further by file extension or name.\n\nThe order of precedence is: ripgrep's automatic filters (ignore files, binary detection, hidden files) first, then glob patterns (including type globs). This means even if you use `--type rust` to include only Rust files, ripgrep will still exclude those that match your `.gitignore`.\n\n## When to use types vs. globs\n\nFor one-off searches, you might wonder whether to use `--type python` or `-g '*.py'`. If you already know the file extension pattern, globs are simpler and require no configuration. But types shine when:\n\n- The type matches multiple extensions (like `c` for `*.c`, `*.h`, and `*.H`)\n- You search the same type repeatedly (type names are easier to remember and type)\n- You want to share your search commands with teammates (they'll understand `--type go` more readily than `-g '*go'`)\n- You need configuration-file persistence (types in `~/.ripgreprc` work without duplicating `--type-add` flags)\n\n## Summary\n\nFile type filtering in ripgrep is built on three commands:\n\n- **`--type TYPE`** includes only files of a specified type\n- **`--type-not TYPE`** excludes files of a specified type\n- **`--type-list`** shows all available types and their globs\n- **`--type-add TYPESPEC`** creates a temporary custom type for the current command\n\nFor types you use frequently, define them once in your ripgrep configuration file and never think about file patterns again. For everything else, `--type-list` is one command away.",
        "filesRead": [
          "GUIDE.md",
          "README.md",
          "crates/ignore/src/default_types.rs"
        ],
        "toolCalls": {
          "Bash": 10,
          "Read": 4,
          "Write": 1
        },
        "transcript": "claude-haiku-4-5--end-user--bare.json",
        "messages": 34,
        "durationMs": 70290,
        "inputTokens": 895006,
        "outputTokens": 4681,
        "costUsd": 0.23524389999999998,
        "model": "claude-haiku-4-5-20251001",
        "sandboxed": true
      },
      "governed": {
        "text": "# ripgrep File-Type Filtering\n\nWhen you search across a large codebase or project, most of your results come from the files you actually want to search. ripgrep offers file-type filtering—a way to define reusable names for sets of file patterns—so you don't have to type the same glob patterns repeatedly.\n\n## What is a file type?\n\nA file type is a named set of glob patterns. ripgrep ships with dozens of predefined types (rust, python, java, markdown, etc.), each corresponding to a file extension or set of extensions. The `c` type, for example, matches both `*.c` and `*.h` files, so you don't have to write `-g '*.{c,h}'` every time.\n\nFile-type filtering sits between glob patterns and your search. Both are optional: you can search without filters at all, use globs alone, use types alone, or combine them. When you do combine them, glob patterns have higher precedence; type filters are lower priority and won't override what you've explicitly specified with globs.\n\n## The --type flag: include specific types\n\nUse `--type` (or `-t` for short) to search only files matching a particular type:\n\n```\n$ rg 'fn run' --type rust\n```\n\nThis searches only Rust source files. You can combine multiple types by repeating the flag:\n\n```\n$ rg 'TODO' --type rust --type python\n```\n\nThis searches both Rust and Python files for the pattern `TODO`.\n\nShort-form usage combines flags: `-trust -tpython` is equivalent to `--type rust --type python`. You can also chain single-letter flags on one dash: `-trust` selects the rust type.\n\n## The --type-not flag: exclude specific types\n\nUse `--type-not` (or `-T` for short) to exclude files of a particular type:\n\n```\n$ rg 'config' --type-not json\n```\n\nThis searches everything except JSON files. Like `--type`, you can repeat it:\n\n```\n$ rg 'TODO' --type-not test --type-not vendor\n```\n\nThis searches for TODO comments everywhere except test and vendored-dependency files.\n\nShort form: `-Trust -Tpython` excludes both rust and python types. Similarly, `-Trust` is shorthand for `--type-not rust`.\n\n## The --type-add flag: define custom types\n\nThe predefined types cover common languages and formats, but your codebase might have custom file formats, unusual naming conventions, or a mix of file types you want to search together. The `--type-add` flag lets you define new types on the fly:\n\n```\n$ rg --type-add 'web:*.{html,css,js}' -tweb 'className'\n```\n\nThis defines a new type called `web` matching HTML, CSS, and JavaScript files, then searches only those files for `className`.\n\nYou can add multiple globs to a type by repeating `--type-add`:\n\n```\n$ rg --type-add 'web:*.html' --type-add 'web:*.css' --type-add 'web:*.js' -tweb 'color'\n```\n\nThe `--type-add` flag also supports an `include` directive to compose types from existing ones:\n\n```\n$ rg --type-add 'src:include:cpp,python,markdown' -tsrc 'pattern'\n```\n\nThis creates a `src` type that matches all files matched by the `cpp`, `python`, and `markdown` types. You can add further globs to the same type:\n\n```\n$ rg --type-add 'src:include:cpp,python' --type-add 'src:*.foo' -tsrc 'pattern'\n```\n\nType names must consist only of Unicode letters or numbers; punctuation isn't allowed. One glob can be added per invocation of `--type-add`, but you can use the brace syntax (`*.{html,css,js}`) to match multiple extensions in one glob.\n\n**Important:** `--type-add` only persists for the current command. If you want to use the same type in every ripgrep invocation, see the configuration file approach below.\n\n## The --type-list flag: discover available types\n\nTo see all available types and their glob patterns:\n\n```\n$ rg --type-list\n```\n\nThis prints each type on its own line, followed by a colon and a comma-delimited list of globs. For example:\n\n```\nmake: *.mak, *.mk, GNUmakefile, Gnumakefile, Makefile, gnumakefile, makefile\nrust: *.rs\npython: *.py, *.pyi, *.pyw\n```\n\nIf you've used `--type-add` or `--type-clear`, those changes appear in the list:\n\n```\n$ rg --type-add 'web:*.html' --type-list | grep web\nweb: *.html\n```\n\nThe list is useful for discovering which types ripgrep recognizes and for verifying the exact globs that make up a type before you use it in a search.\n\n## The special \"all\" type\n\nBoth `--type` and `--type-not` support a special value `all`, which acts as a shorthand:\n\n`--type all` is equivalent to `--type agda --type asciidoc --type asm ... ` for every single type ripgrep knows about. It puts ripgrep into \"whitelist\" mode: only files matching at least one type are searched. Unrecognized files are skipped.\n\n`--type-not all` inverts this: only files that *don't* match any known type are searched. This is \"blacklist\" mode and is useful when you want to search only custom or unusual file formats that ripgrep doesn't recognize.\n\n## Combining file-type filters\n\nYou can mix `--type` and `--type-not` in a single command:\n\n```\n$ rg 'TODO' --type rust --type python --type-not test\n```\n\nThis searches Rust and Python files, but excludes any files matching the test type.\n\nWhen both whitelist and blacklist filters are present, the whitelist takes precedence: a file must match at least one type you've selected, and must not match any type you've negated.\n\n## Making types persistent\n\nEach invocation of `--type-add` only lasts for that one command. To use the same custom type across multiple searches, add it to your ripgrep configuration file. Set the `RIPGREP_CONFIG_PATH` environment variable to point to your config file, then add `--type-add` entries:\n\n```\n# ~/.ripgreprc\n--type-add\nweb:*.{html,css,js}\n\n--type-add\nconfig:*.ini,*.toml,*.yaml,*.yml\n```\n\nConfiguration file syntax requires each flag and its value on separate lines. Every line becomes a single argument to ripgrep, so `--type-add web:*.html` won't work; you must write it across two lines. Comments start with `#`.\n\nripgrep prepends your config file to its arguments before parsing, so command-line arguments override the config. Running `rg --type-add 'web:*.{html,css,js,md}' -tweb` will use your command-line definition of `web`, not the one from the config.\n\n## Precedence and limits\n\nFile-type filters have lower precedence than glob patterns. If you write:\n\n```\n$ rg 'pattern' --type rust -g '!*.rs'\n```\n\nThe glob negation wins, and no Rust files are searched. Globs also take precedence over `.gitignore` files and other automatic filtering.\n\nType filters never modify what ripgrep considers binary files, hidden files, or files in ignore patterns. A type filter says, \"search only files of this type,\" but those files must still pass ripgrep's other filters. If a Rust file is in `.gitignore`, it won't be searched, even if you use `--type rust`.\n\n## Common patterns\n\nSearch a single language:\n\n```\n$ rg 'TODO' --type python\n```\n\nSearch multiple related languages:\n\n```\n$ rg 'import' --type rust --type go\n```\n\nExclude a category of files:\n\n```\n$ rg 'function' --type-not test\n```\n\nSearch only recognized file types (no stray files):\n\n```\n$ rg 'query' --type all\n```\n\nSearch only unknown file types:\n\n```\n$ rg 'data' --type-not all\n```\n\nDefine a custom type for the session:\n\n```\n$ rg --type-add 'logs:*.log' -tlogs 'ERROR'\n```",
        "filesRead": [
          "GUIDE.md",
          "README.md",
          "crates/ignore/src/types.rs",
          "crates/ignore/src/default_types.rs",
          "crates/core/main.rs",
          "crates/core/flags/defs.rs"
        ],
        "toolCalls": {
          "Bash": 11,
          "Read": 7,
          "Write": 1
        },
        "transcript": "claude-haiku-4-5--end-user--governed.json",
        "messages": 42,
        "durationMs": 84374,
        "inputTokens": 1334300,
        "outputTokens": 5765,
        "costUsd": 0.3147865000000001,
        "model": "claude-haiku-4-5-20251001",
        "sandboxed": true
      },
      "bareFile": "claude-haiku-4-5/end-user/bare.md",
      "governedFile": "claude-haiku-4-5/end-user/governed.md"
    }
  ]
}
