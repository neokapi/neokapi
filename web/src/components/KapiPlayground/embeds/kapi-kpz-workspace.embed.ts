// GENERATED by scripts/walkthrough-gen from web/walkthroughs/kapi-kpz-workspace.scene.yaml.
// Do not edit by hand — change the .scene.yaml and regenerate.
import type { WalkthroughEmbedConfig } from "./types";

const config: WalkthroughEmbedConfig = {
  id: "kapi-kpz-workspace",
  scene: "kpz-workspace",
  mode: "interactive",
  seed: ["messages.json"],
  files: [],
  steps: [
    {
      command: "kapi extract messages.json -o work.kpz --target-lang qps",
      narration:
        "extract reads the source into a portable .kpz and records a recipe (locales + output layout).",
    },
    {
      command: "kapi pseudo-translate work.kpz",
      narration:
        "The tool stores its changes in the working cache. The .kpz file is updated when you pack it.",
    },
    {
      command: "kapi info work.kpz",
      narration:
        "info reports a dirty workspace because the working cache contains unpacked changes.",
    },
    {
      command: "kapi pack work.kpz",
      narration:
        "pack writes the changes to the .kpz bundle. info would now report a clean workspace.",
    },
    {
      command: "kapi merge work.kpz -o out/",
      narration:
        "merge writes the translated documents from the workspace, one per source and locale.",
    },
  ],
};

export default config;
