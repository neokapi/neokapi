// GENERATED by scripts/walkthrough-gen from web/walkthroughs/kapi-pseudo-translate.scene.yaml.
// Do not edit by hand — change the .scene.yaml and regenerate.
import type { WalkthroughEmbedConfig } from "./types";

const config: WalkthroughEmbedConfig = {
  id: "kapi-pseudo-translate",
  scene: "pseudo-translate",
  mode: "interactive",
  seed: ["messages.json"],
  files: [],
  steps: [
    {
      command: "kapi pseudo-translate messages.json -o messages.fr.json",
      narration:
        "Expands every string with locale-shaped accent characters and length padding, so truncation, concatenation, and hardcoded-string bugs surface before any real translation is bought. No AI provider, no keys.",
    },
  ],
};

export default config;
