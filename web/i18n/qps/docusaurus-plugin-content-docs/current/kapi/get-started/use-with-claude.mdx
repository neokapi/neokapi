---
title: Use with Claude
description: Two directions — work in Claude while it drives kapi (Agent Skill, MCP), or let kapi run Claude through your local Claude Code sign-in, no API key needed.
keywords: [kapi, claude, claude code, claude subscription, agent skills, mcp, model context protocol, kapi models setup, voice profile, terminology, CI]
---

# ▒ Üšé ŵîţĥ Çļàüđé ▒

▒ ķàþî àñđ Çļàüđé çöññéçţ îñ **ţŵö đîŕéçţîöñš**. Îñ öñé, ýöü ŵöŕķ îñ Çļàüđé àñđ îţ đŕîṽéš ķàþî — ţĥé àššîšţàñţ ŵŕîţéš àñđ ţŕàñšļàţéš îñšîđé ķàþî'š ĝüàŕđŕàîļš. Îñ ţĥé öţĥéŕ, ķàþî ŕüñš Çļàüđé: ýöüŕ ļöçàļ Çļàüđé Çöđé šîĝñ-îñ ƃéçöḿéš ķàþî'š ÀÎ þŕöṽîđéŕ, šö `ķàþî üþ` àñđ `ķàþî ţŕàñšļàţé` ŕüñ öñ ýöüŕ Çļàüđé šüƃšçŕîþţîöñ ŵîţĥ ñö ÀÞÎ ķéý. Šţàŕţ ƒŕöḿ ŵĥàţ ýöü ĥàṽé: ▒

| You have… | Use |
|---|---|
| **Claude Pro or Max** (Claude Code installed) | Both directions. [Work in Claude](#work-in-claude) via the skill or MCP; run `kapi models setup` and [kapi runs Claude](#let-kapi-run-claude) on your subscription — no key. |
| **An API key** (Anthropic, OpenAI, Gemini) | `kapi models setup` finds keys already in your environment, or stores one in the OS keychain, and verifies it with a test call. |
| **Nothing, or offline** | A local model via [`kapi models ollama`](/kapi/recipes/choose-a-translation-provider), or Kapi Desktop's demo engine (illustrative output, no AI). |

## ▒ Ŵöŕķ îñ Çļàüđé ▒

▒ Ýöü đéšçŕîƃé ţĥé öüţçöḿé; Çļàüđé ŕüñš ķàþî. ķàþî šüþþļîéš ţĥé ĝüàŕđŕàîļš — ţĥé ƒàîţĥƒüļ ŕöüñđ-ţŕîþ, ṽöîçé þŕöƒîļé, ţéŕḿîñöļöĝý, ţĥé çĥéçķ ĝàţéš — àñđ Çļàüđé đöéš ţĥé ŵŕîţîñĝ îñšîđé ţĥéḿ. Àļļ þàţĥš ñééđ `ķàþî` öñ ýöüŕ `ÞÀŢĤ` ([îñšţàļļàţîöñ](/kapi/get-started/installation)); ñö þŕöṽîđéŕ çŕéđéñţîàļ îš ŕéǫüîŕéđ ƒöŕ àţţéñđéđ ŵöŕķ — Çļàüđé ŵŕîţéš, ķàþî àþþļîéš àñđ çĥéçķš, ñö šéçöñđ ḿöđéļ. ▒

| You work in… | Use | How it drives kapi |
|---|---|---|
| **Chat** — Claude Code, plain-language prompts | the **Agent Skill** (`SKILL.md`) | Claude runs `kapi` commands itself and loops until the gates pass |
| **IDE** — Claude Desktop, Cursor, Windsurf, VS Code | the **MCP server** (`kapi mcp`) | structured tools: `extract_content`, `apply_edits`, `check_file`, `voice_check` |
| **CI** — no assistant in the loop | the **CLI** directly | `kapi check` / `kapi check --ship` exit non-zero, failing the build |

<details>
<summary>Install the Agent Skill (Claude Code and any SKILL.md-aware tool)</summary>

**Claude Code** — the plugin bundles the skill plus two hooks:

```text
/plugin marketplace add neokapi/claude-plugins
/plugin install kapi@neokapi-plugins
```

The **Stop hook** runs the project gate (`kapi check --ship`) when Claude tries to finish and keeps it working while a gate fails; the **PreToolUse hook** denies hand-edits to generated translation targets, steering changes through the source. Both are fail-open outside a project, and both follow one [decision protocol](#the-hook-decision-protocol).

**Copilot, Cursor, Windsurf, VS Code, Codex** — `SKILL.md` is an open standard:

```bash
npx skills add neokapi/agent-skills --skill kapi     # add -a <tool> for a specific tool
npx skills update kapi                               # refresh later
```

</details>

<details>
<summary>Install the MCP server (Claude Desktop, Claude Code, Cursor, and the rest)</summary>

Your client launches `kapi mcp` as a stdio subprocess. For Claude Code, add `.mcp.json` at the repository root (Claude Desktop uses `claude_desktop_config.json`, Copilot `.vscode/mcp.json` — same shape):

```json
{
  "mcpServers": {
    "kapi": { "command": "kapi", "args": ["mcp"] }
  }
}
```

See [MCP server](/reference/mcp) for every client's configuration and the full tool list.

</details>

### ▒ Ţĥé ĥööķ đéçîšîöñ þŕöţöçöļ {#the-hook-decision-protocol} ▒

▒ À ķàþî ĥööķ šţàţéš îţš ṽéŕđîçţ àš ĴŠÖÑ öñ šţđöüţ àñđ àļŵàýš éẋîţš `0`. Ţĥé éẋîţ çöđé îš ñöţ ţĥé çĥàññéļ: Çļàüđé Çöđé ŕéàđš à ñöñ-žéŕö éẋîţ àš à *ƃŕöķéñ ĥööķ*, šö à đéñîàļ çàŕŕîéđ ţĥàţ ŵàý ŵöüļđ ƃé đöŵñĝŕàđéđ ţö à ĥööķ éŕŕöŕ ŕàţĥéŕ ţĥàñ éñƒöŕçéđ. `ķàþî çĥéçķ --šĥîþ` ķééþš îţš öŵñ ĝàţé éẋîţ çöđé (`3`) ŵĥéñ ýöü ŕüñ îţ ýöüŕšéļƒ — ţĥé ĥööķ đŕîṽéš ţĥàţ ĝàţé ƃüţ îš ñöţ îţ. ▒

| What the hook found | On stdout | What happens |
|---|---|---|
| A gate fails / the file is a generated target | `{"decision":"block","reason":"…"}` (Stop) · `{"hookSpecificOutput":{"permissionDecision":"deny","permissionDecisionReason":"…"}}` (PreToolUse) | Claude keeps working, or the edit is refused. The `reason` names the findings, or the source and locale to change instead. |
| Nothing to report — gates pass, the file is not generated, or there is no kapi project | nothing | The operation proceeds. |
| **The guard could not run at all** | `{"systemMessage":"…"}` — plus the same warning on stderr, naming the hook | The operation proceeds: kapi hooks fail open, so a broken guard never blocks work. But the bypass is stated, because a guard that never ran must not look like a guard that passed. |

▒ Ţĥé ţĥîŕđ ŕöŵ îš ţĥé öñé ŵöŕţĥ ķñöŵîñĝ àƃöüţ. Àñ üñŕéàđàƃļé, éḿþţý, öŕ ḿàļƒöŕḿéđ ĥööķ þàýļöàđ; à šéššîöñ đîŕéçţöŕý ķàþî çàññöţ éñţéŕ; à ŕéçîþé ţĥàţ ŵîļļ ñöţ ļöàđ ƃéçàüšé îţ ñééđš à þļüĝîñ ýöü ĥàṽé ñöţ îñšţàļļéđ — éàçĥ öƒ ţĥöšé ļéàṽéš ţĥé ĝüàŕđ îñšţàļļéđ ƃüţ îñéŕţ. Ţĥé ñöţîçé šàýš ŵĥîçĥ ĥööķ, àñđ ŵĥàţ šţöþþéđ îţ. ▒

▒ Ţĥé ŵîŕé šĥàþéš àŕé Çļàüđé Çöđé'š, àñđ Çļàüđé Çöđé ṽéŕšîöñš îţš öŵñ ĥööķ šçĥéḿà; ķàþî đöéš ñöţ àđđ à ṽéŕšîöñ öƒ îţš öŵñ. Ŵĥàţ îš šţàƃļé ĥéŕé îš ţĥé þöļîçý îñ ţĥé ţàƃļé: ƒàîļ öþéñ, àñđ ñéṽéŕ šîļéñţļý. ▒

▒ Ţĥé ļööþ, ŵĥîçĥéṽéŕ đööŕ: Çļàüđé þüļļš ýöüŕ ṽöîçé ĝüîđé àñđ àþþŕöṽéđ ţéŕḿš ƒîŕšţ (`ķàþî ṽöîçé ĝüîđé`, `ķàþî ţéŕḿš ļööķüþ`), ŵŕîţéš ţĥŕöüĝĥ ţĥé öñé ŵŕîţé ṽéŕƃ (`ķàþî àþþļý` — šţŕüçţüŕé þŕéšéŕṽéđ, đŕîƒţ-ĝüàŕđéđ ƃý çöñţéñţ ĥàšĥ), ţĥéñ ĝàţéš öüţ ŵîţĥ `ķàþî çĥéçķ` üñţîļ ĝŕééñ. Ƃîñđ ţĥé þŕöƒîļé àñđ ţĥé ţéŕḿš šţöŕé îñ à [þŕöĵéçţ](/kapi/get-started/first-project) àñđ ţĥîš ĥàþþéñš àüţöḿàţîçàļļý; ýöüŕ þŕöḿþţš šţàý šĥöŕţ — "Šéţ ķàþî üþ ƒöŕ ţĥîš þŕöĵéçţ", "Çĥéçķ `ŔÉÀĐḾÉ.ḿđ` àĝàîñšţ öüŕ ṽöîçé þŕöƒîļé àñđ ƒîẋ ŵĥàţ ƒàîļš", "Ţŕàñšļàţé ţĥé đöçš îñţö Ƒŕéñçĥ àñđ Ĝéŕḿàñ, ţĥéñ ƃŕîñĝ ţĥéḿ ţö ţĥé šĥîþ ĝàţé". ▒

## ▒ Ļéţ ķàþî ŕüñ Çļàüđé ▒

▒ Ƒöŕ üñàţţéñđéđ ŕüñš — `ķàþî üþ` àçŕöšš éṽéŕý ļöçàļé, `ķàþî ţŕàñšļàţé` ŵîţĥöüţ ýöü îñ ţĥé ļööþ — ķàþî ñééđš à þŕöṽîđéŕ öƒ îţš öŵñ. Îƒ Çļàüđé Çöđé îš šîĝñéđ îñ öñ ýöüŕ ḿàçĥîñé, ţĥàţ þŕöṽîđéŕ çàñ ƃé ýöüŕ Çļàüđé šüƃšçŕîþţîöñ: ▒

```bash
kapi models setup     # detects the Claude Code CLI — "uses your Claude subscription" — one confirm, no key
```

▒ Šéţüþ ṽéŕîƒîéš ţĥé çĥöîçé ŵîţĥ à šḿàļļ ţéšţ çàļļ àñđ ŵŕîţéš ţĥé đéƒàüļţ þŕöṽîđéŕ ţö ţĥé ĝļöƃàļ çöñƒîĝ, šĥàŕéđ ŵîţĥ Ķàþî Đéšķţöþ — ŵĥöšé ƒîŕšţ-öþéñ **Çöññéçţ ýöüŕ ÀÎ** çàŕđ öƒƒéŕš ţĥé šàḿé đéţéçţéđ šîĝñ-îñ àš à öñé-çļîçķ ƃüţţöñ. `ķàþî ḿöđéļš ļîšţ` šĥöŵš îţ üñđéŕ *Đéţéçţéđ*, àļöñĝšîđé Öļļàḿà ḿöđéļš àñđ àñý çļöüđ ķéýš. Šüƃšçŕîþţîöñ üšé îš ŵîñđöŵ-ļîḿîţéđ: à ĥéàṽý ŕüñ ţĥàţ éẋĥàüšţš ýöüŕ Çļàüđé üšàĝé ŵîñđöŵ þàŕķš ţĥé ŕéḿàîñđéŕ àñđ ŕéþöŕţš ŵĥéñ îţ ŕéšüḿéš ("Çļàüđé šüƃšçŕîþţîöñ ļîḿîţ ŕéàçĥéđ — ŕéšüḿéš ~7þḿ"); ţĥé ñéẋţ `ķàþî üþ` þîçķš üþ éẋàçţļý ţĥé üñîţš šţîļļ þéñđîñĝ. Ƒöŕ üñîñţéŕŕüþţéđ öŕ ÇÎ ŕüñš üšé àñ ÀÞÎ ķéý îñšţéàđ — šüƃšçŕîþţîöñ àüţĥ îš ƃöüñđ ţö ýöüŕ þéŕšöñàļ šîĝñ-îñ àñđ îţš üšàĝé ŵîñđöŵ, šö **ÇÎ šĥöüļđ àļŵàýš üšé à ķéý** (à þŕöṽîđéŕ éñṽ ṽàŕ, öŕ `ķàþî çŕéđéñţîàļš àđđ`). Šéé [Šĥîþ ĝàţéš & ÇÎ](/kapi/recipes/ship-gates-and-ci). ▒

## ▒ Ñéẋţ ▒

- ▒ [Çöñţéñţ ĝöṽéŕñàñçé ƒöŕ ÀÎ](/kapi/recipes/content-governance-for-ai) — ţĥé ĝüàŕđŕàîļš ţĥàţ ḿàķé àĝéñţ öüţþüţ þàšš. ▒
- ▒ [Ƃüļķ ŕéŵŕîţé ŵîţĥ àñ àĝéñţ](/kapi/recipes/bulk-rewrite-with-an-agent) — à ŕéþö-ŵîđé šŵééþ, ĝàţéđ. ▒
- ▒ [Çĥööšé à ḿöđéļ](/kapi/recipes/choose-a-translation-provider) — éṽéŕý þŕöṽîđéŕ ķàþî çàñ ŕüñ. ▒
