---
id: rewrite-content
title: "Edit content in any format, faithfully"
sidebar_label: "Edit content"
description: "Read a file's blocks, rewrite the text yourself, and write it back with kapi apply — the format, structure, and non-text elements stay byte-for-byte intact, and no model is involved."
hide_table_of_contents: true
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import { PendingMedia } from "@neokapi/docs-shared";

# ▒ Éđîţ çöñţéñţ îñ àñý ƒöŕḿàţ, ƒàîţĥƒüļļý ▒

▒ **Ĝöàļ:** éđîţ ţĥé ţéẋţ îñšîđé à ƒîļé ŵîţĥöüţ ţöüçĥîñĝ îţš šţŕüçţüŕé — à Ŵöŕđ đöçüḿéñţ, à ĴŠÖÑ çàţàļöĝ, à Ḿàŕķđöŵñ ĝüîđé, öŕ àñý öţĥéŕ ƒöŕḿàţ ķàþî ŕéàđš. Ţĥé ŕéšüļţ îš ţĥé šàḿé ƒîļé — šàḿé ķéýš, šàḿé šţýļéš, šàḿé îñļîñé çöđéš — ŵîţĥ öñļý ţĥé þŕöšé çĥàñĝéđ. Ýöü (öŕ ýöüŕ àššîšţàñţ) đö ţĥé ŕéŵŕîţîñĝ; ķàþî þàŕšéš ţĥé ƒöŕḿàţ, éñƒöŕçéš à ƒàîţĥƒüļ ŕöüñđ-ţŕîþ, àñđ ŵŕîţéš ţĥé ŕéšüļţ ƃàçķ. ķàþî ñéṽéŕ šéñđš çöñţéñţ ţö à ḿöđéļ ţö ŕéŵŕîţé îţ, àñđ ñö þŕöĵéçţ, þŕöṽîđéŕ, öŕ çŕéđéñţîàļ îš ñééđéđ. ▒

<PendingMedia
  kind="video"
  scene="web/walkthroughs/kapi-rewrite-content.scene.yaml"
>
  A short walkthrough inspecting a file's blocks, previewing an edit as a diff,
  applying it in place, and verifying the result with a check — showing how only
  the leaf text changes while the format round-trips.
</PendingMedia>

<Tabs groupId="surface" queryString>
<TabItem value="desktop" label="Desktop" default>

The desktop shows the blocks and verifies the result; the write itself is the CLI's or an assistant's `apply`.

1. **Read the blocks.** Open the file in your project — **Blocks** lists each text block as the engine reads it; **Preview** shows the rendered document with the source ↔ target toggle.
2. **Edit translated text in Review.** For target-language units the edit surface is the [Review queue](/kapi/recipes/review-and-approve): edit, then approve — the write lands through the same faithful round-trip.
3. **Run source rewrites through `apply`.** Drive the inspect → `apply` loop from the CLI tab or a [connected assistant](/kapi/get-started/use-with-claude); then reopen **Preview** to see that headings, styles, and inline codes survived, and re-run the **Checks** view to confirm the gate.

</TabItem>
<TabItem value="agent" label="Agent (Claude)">

With kapi wired as an [MCP server](/reference/mcp) or through the [Agent Skill](/kapi/get-started/use-with-claude), ask:

> Simplify the setup section of `guide.md` — shorter sentences, same meaning, keep every link.

The assistant drives the loop through structured calls — `extract_content` to read blocks, `apply_edits` to write the change-set back, `check_file` to confirm the gate. The assistant is the writer; kapi enforces the round-trip, drift-checks each edit by `content_hash`, and rejects an edit that would corrupt inline markup. No model is called for the write itself.

</TabItem>
<TabItem value="cli" label="CLI">

**Read the blocks.** `kapi inspect` returns one record per content block — the `text`, the block's structural role, a stable `id`, and a `content_hash`:

```bash
kapi inspect guide.md --jsonl        # one JSON object per line
```

Two fields anchor an edit: **`text`** renders inline codes as `<x id="…"/>` tokens — keep every token in your edited text; a placeholder is `<x id="1/"/>`, a paired span opens with `<x id="1"/>` and closes with `<x id="/1"/>`. **`content_hash`** is the block's identity — send it back so kapi can tell the block is still the one you read.

**Edit, then apply.** Produce one `content` entry per changed block; `kapi apply` is the one write verb:

```json
{
  "kind": "content",
  "file": "guide.md",
  "id": "p3",
  "content_hash": "a1b2c3…",
  "text": "See the <x id=\"1/\"/> dashboard."
}
```

```bash
kapi inspect guide.md --jsonl > blocks.jsonl
# rewrite the "text" of the blocks you want changed — keeping every <x id="…"/>
# token — and save them as content entries to edits.jsonl (you do this; no command)
kapi apply edits.jsonl --diff             # preview as a unified diff, write nothing
kapi apply edits.jsonl                    # apply in place
kapi apply edits.jsonl --in-place=.bak    # apply, keeping a .bak
```

**Verify.** `kapi check` confirms the result meets your content rules — deterministic, read-only, exit `0` pass and `3` on gate failure:

```bash
kapi check guide.md --max-chars 500 --forbid "utilize"
```

</TabItem>
</Tabs>

## ▒ Ţĥé ţŵö ĝüàŕđš ▒

▒ `àþþļý` ŵŕîţéš à ƃļöçķ öñļý ŵĥéñ ƃöţĥ ĝüàŕđš þàšš; à ƃļöçķéđ éđîţ ļéàṽéš ţĥàţ ƃļöçķ üñţöüçĥéđ àñđ îš ŕéþöŕţéđ, šö ñöţĥîñĝ îš šîļéñţļý çöŕŕüþţéđ: ▒

- ▒ **Đŕîƒţ ĝüàŕđ.** À ƃļöçķ ŵĥöšé çüŕŕéñţ `çöñţéñţ_ĥàšĥ` ñö ļöñĝéŕ ḿàţçĥéš ýöüŕ éñţŕý çĥàñĝéđ šîñçé ýöü îñšþéçţéđ îţ — ţĥé éđîţ îš ḿàŕķéđ **šţàļé** àñđ šķîþþéđ. ▒
- ▒ **Îñļîñé-çöđé ĝüàŕđ.** Àñ éđîţéđ `ţéẋţ` ţĥàţ đŕöþš, îñṽéñţš, đüþļîçàţéš, öŕ üñƃàļàñçéš àñ `<ẋ îđ="…"/>` ţöķéñ îš **ŕéĵéçţéđ** ŕàţĥéŕ ţĥàñ ŵŕîţţéñ ƃàçķ ŵîţĥ ƃŕöķéñ ḿàŕķüþ. ▒

▒ Éîţĥéŕ öüţçöḿé éẋîţš öñ ţĥé ĝàţé çöđé (`3`), đîšţîñçţ ƒŕöḿ àñ öþéŕàţîöñàļ éŕŕöŕ — ŕé-îñšþéçţ ţĥé àƒƒéçţéđ ƃļöçķš àñđ ŕéţŕý ŵîţĥ ƒŕéšĥ ĥàšĥéš. `àþþļý` îš îđéḿþöţéñţ, àñđ ţĥé šàḿé çĥàñĝé-šéţ çàñ çàŕŕý àššéţ éđîţš (àñ àþþŕöṽéđ ţéŕḿ, à ṽöîçé ŕüļé — šéé [Çŕéàţé çöñţéñţ](/kapi/recipes/create-content)) öŕ à `ķîñđ:"ŕéṽîéŵ"` éñţŕý ([Ţĥé ķàþî ļööþ](/kapi/convergence#review-the-state-record)) — öñé ŵŕîţé ṽéŕƃ, đîƒƒéŕéñţ `ķîñđ`š. ▒

## ▒ Ŵĥàţ "ƒàîţĥƒüļļý" ḿéàñš ▒

▒ Öñļý ţĥé éẋţŕàçţéđ þŕöšé çĥàñĝéš; éṽéŕýţĥîñĝ öüţšîđé îţ — `.đöçẋ` çĥàŕàçţéŕ šţýļéš àñđ ţàƃļé ƃöŕđéŕš, ĴŠÖÑ ķéýš àñđ ñöñ-šţŕîñĝ ṽàļüéš, Ḿàŕķđöŵñ ĥéàđîñĝš àñđ ƒéñçéš — îš ŕéþŕöđüçéđ ƃýţé ƒöŕ ƃýţé. Ţĥîš îš ŵĥàţ ḿàķéš éđîţîñĝ ƃîñàŕý öƒƒîçé ƒöŕḿàţš (`.đöçẋ`, `.þþţẋ`) šàƒé; `ķàþî ƒöŕḿàţš` ḿàŕķš šüçĥ ƒöŕḿàţš `ƒàîţĥƒüļ` îñ îţš Éđîţ çöļüḿñ. ▒

## ▒ Îñ à þŕöĵéçţ ▒

▒ Îñ à [`.ķàþî` þŕöĵéçţ](/kapi/get-started/first-project), `ķàþî îñšþéçţ` àñđ `ķàþî çĥéçķ` ŕüñ àĝàîñšţ ţĥé ŕéçîþé'š đéçļàŕéđ çöñţéñţ ŵîţĥ ñö ƒîļé àŕĝüḿéñţ, àñđ `ķàþî àþþļý` ļàñđš éđîţš àĝàîñšţ ţĥé ñàḿéđ ƒîļéš. ▒

## ▒ Ñéẋţ ▒

- ▒ [Çĥéçķš](/framework/checks) — ŕüļé ƒàḿîļîéš, þŕöƒîļéš, šçöŕîñĝ, àñđ ţĥé ĝàţé éẋîţ çöđéš. ▒
- ▒ [Çŕéàţé çöñţéñţ](/kapi/recipes/create-content) — ţĥé àüţĥöŕ → þàŕšé → çĥéçķ ļööþ àñđ ţĥé ḿîẋéđ çĥàñĝé-šéţ. ▒
- ▒ [Ƒééđ çöñţéñţ ţö ýöüŕ ÀÎ](/kapi/recipes/ingest-for-ai) — ţĥé šàḿé àñçĥöŕéđ ƃļöçķš àš àñ îñĝéšţîöñ šţŕéàḿ. ▒
