---
id: bulk-rewrite-with-an-agent
title: "Bulk rewrite with an agent"
sidebar_label: "Bulk rewrite with an agent"
description: "Sweep a rewrite across a whole content set — rename a product, retire a term, change tense — with an AI agent doing the writing and kapi guaranteeing that only the text changes."
hide_table_of_contents: true
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# ▒ Ƃüļķ ŕéŵŕîţé ŵîţĥ àñ àĝéñţ ▒

▒ **Ĝöàļ:** àþþļý öñé éđîţöŕîàļ çĥàñĝé éṽéŕýŵĥéŕé — ŕéñàḿé à þŕöđüçţ, ŕéţîŕé à þĥŕàšé, šöƒţéñ à ţéñšé — àçŕöšš ƒîļéš ýöüŕ éđîţöŕ çàñ'ţ éṽéñ öþéñ (`.đöçẋ`, çàţàļöĝš, ĤŢḾĻ, Ḿàŕķđöŵñ), ŵîţĥöüţ ŕîšķîñĝ šţŕüçţüŕé. Ţĥé đîṽîšîöñ öƒ ļàƃöŕ: ţĥé àĝéñţ đéçîđéš ŵĥàţ ţĥé ñéŵ ţéẋţ šàýš; ķàþî ĝüàŕàñţééš éṽéŕýţĥîñĝ éļšé — îñļîñé çöđéš þŕéšéŕṽéđ, đŕîƒţ-ĝüàŕđéđ ŵŕîţéš, àñđ à çĥéçķ ĝàţé ţĥàţ þŕöṽéš ţĥé šŵééþ îš đöñé. Ñö šéçöñđ ḿöđéļ îš îñṽöļṽéđ îñ ţĥé ŵŕîţé. ▒

<Tabs groupId="surface" queryString>
<TabItem value="desktop" label="Desktop" default>

The sweep itself is agent- or CLI-driven; Desktop is where you verify it:

1. **Before:** bind the rule that motivates the sweep — a forbidden term, a required phrasing — in the project's voice profile, so the **Checks** view shows exactly which blocks are affected and how many.
2. **After:** re-run the Checks view to confirm the count is zero, and spot-check the edited files in the preview — structure, styles, and placeholders intact.

For translated content, an edited source drops affected units back to *pending*; **Bring up to date** re-translates just those, and the [Review queue](/kapi/recipes/review-and-approve) picks up anything that parks.

</TabItem>
<TabItem value="agent" label="Agent (Claude)">

State the change once, over the whole set:

> We renamed "Acme Sync" to "Acme Flow". Update every mention across `docs/` and `src/locales/en/` — including the `.docx` one-pagers — and keep the formatting.

The assistant runs the loop at scale:

1. `kapi inspect --jsonl` over the set — one record per block, with `id`, `content_hash`, and the text (inline codes as `<x id="…"/>` placeholders).
2. It rewrites only the blocks that mention the term — judgment calls included ("Acme Sync's engine" → "the Acme Flow engine"), which is why this is an agent job and not a `sed` job.
3. `kapi apply` lands the change-set; any block whose hash drifted since the read is left unchanged and reported, so a concurrent edit can't be clobbered.
4. `kapi check --forbid "Acme Sync"` proves completion — exit 0 means the sweep is actually done, not probably done.

Make the retired term a permanent [voice vocabulary rule](/kapi/recipes/keep-source-on-brand) in the same change-set, and the old name can never quietly return.

</TabItem>
<TabItem value="cli" label="CLI">

The same loop with you (or a script) as the writer:

```bash
kapi inspect docs/**/*.md report.docx --jsonl > blocks.jsonl
# rewrite the matching blocks' "text" into edits.jsonl — content entries
# carrying each block's id + content_hash; keep the <x id="…"/> tags
kapi apply edits.jsonl --diff            # preview the whole sweep as one diff
kapi apply edits.jsonl --in-place=.bak   # land it, keeping backups
kapi check docs/**/*.md --forbid "Acme Sync"   # prove it's done
```

For pure vocabulary substitution with zero judgment involved, `kapi voice rewrite` does it deterministically from a profile — no agent, no model. The change-set shape and the round-trip guards are in [Edit content](/kapi/recipes/rewrite-content).

</TabItem>
</Tabs>

## ▒ Ñéẋţ ▒

- ▒ [Éđîţ çöñţéñţ](/kapi/recipes/rewrite-content) — ţĥé îñšþéçţ → éđîţ → àþþļý ļööþ àñđ îţš ĝüàŕđš, îñ đéþţĥ. ▒
- ▒ [Çöñţéñţ ĝöṽéŕñàñçé ƒöŕ ÀÎ](/kapi/recipes/content-governance-for-ai) — ţĥé šţàñđîñĝ ŕüļéš ţĥàţ ķééþ ƒüţüŕé öüţþüţ çļéàñ. ▒
- ▒ [Üšé ŵîţĥ Çļàüđé](/kapi/get-started/use-with-claude) — ŵîŕîñĝ ţĥé àššîšţàñţ ţĥàţ ŕüñš ţĥîš ļööþ. ▒
