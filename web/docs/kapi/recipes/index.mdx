---
id: index
slug: /kapi/cli
title: Kapi CLI recipes
sidebar_label: Overview
sidebar_position: 0
description: "Task-by-task recipes for the kapi CLI — estimate a job, catch UI truncation, enforce terminology, pre-translate with TM — each runnable in the browser, with its ad-hoc-or-project mode marked up front."
keywords: [kapi, CLI, recipes, ad-hoc, project, format-aware localization, checks]
hide_table_of_contents: true
---

import Link from "@docusaurus/Link";
import KapiGuidedEmbed from "@site/src/components/KapiPlayground/KapiGuidedEmbed";
import { ContentLab } from "@site/src/components/Lab";

# Kapi CLI recipes

Each recipe takes a single task — estimate a job, catch UI truncation, enforce a
glossary — states the goal, walks the steps, and lets you run the commands
yourself. The interactive embeds run the real `kapi` binary in your browser: no
install, no server, no API key. Every command shown is the command that runs.

For what kapi is and how the CLI and Kapi Desktop relate, see the
[Kapi overview](/kapi/overview). For every command and flag, see the
[command reference](/commands).

## Get oriented

`kapi` runs entirely in your browser here — no install, no server. Three commands
orient you: `kapi --help` lists the top-level commands, `kapi formats` enumerates
the format readers, and `kapi tools` lists the processing tools you can run on
extracted content. Step through the tour from the rail, or press **Run all**.

<KapiGuidedEmbed id="kapi-overview" />

The explorer below shows the format engine round-trip: run a file through
`pseudo-translate` and compare the source with the output — only the leaf text
changes, while the non-translatable skeleton (tags, keys, attributes) returns
byte-for-byte. This is the same reader and writer the recipes drive.

<ContentLab lessonIds={["roundtrip"]} defaultSampleId="page-html" />

## Ad-hoc or project?

A **`.kapi` project is the recommended working model** for any repository or
content set you localize repeatedly. It captures the languages, content patterns,
and flows in a committed recipe once, adds git-style upward discovery of that
recipe from any subdirectory, and keeps a project-local translation memory and
termbase that every run reuses — so you stop repeating flags and start
accumulating work. [Create your first project](/kapi/get-started/first-project)
walks the setup; the [project file reference](/reference/project-file) covers the
recipe format and discovery rules.

kapi also runs **ad-hoc**: a command operates directly on the files you name,
configured by flags, with nothing saved —
`kapi translate -i messages.json --target-lang fr`. That's the quick path for
one-off jobs, scripting, and CI, and a fine on-ramp. Many recipes here are shown
ad-hoc because they're genuine one-offs; each notes the project-mode equivalent
where it helps.

Every recipe is marked at the top with its mode — **Runs anywhere** (pure ad-hoc,
nothing saved), **Better in a project** (runs ad-hoc too, but a `.kapi` project's
reused translation memory, termbase, and flows make it materially better), or
**Needs a `.kapi` project**. Recipes that run ad-hoc also carry a short **In a
project** section showing the project-mode equivalent.

## Recipes

<div className="row">
  <div className="col col--6 margin-bottom--lg">
    <Link className="card padding--lg" to="/kapi/recipes/estimate-cost">
      <h3>Estimate a translation job</h3>
      <p>Count blocks and source words in any supported format to size up a quote before you commission it.</p>
    </Link>
  </div>
  <div className="col col--6 margin-bottom--lg">
    <Link className="card padding--lg" to="/kapi/recipes/pseudo-translate">
      <h3>Catch UI truncation early</h3>
      <p>Pseudo-translate your strings to surface clipping and missing-string bugs before real text arrives.</p>
    </Link>
  </div>
  <div className="col col--6 margin-bottom--lg">
    <Link className="card padding--lg" to="/kapi/recipes/tm-termbase-storage">
      <h3>Where TM and termbases live</h3>
      <p>The three storage models — loose file, named store, or project store — and the flags that select each.</p>
    </Link>
  </div>
  <div className="col col--6 margin-bottom--lg">
    <Link className="card padding--lg" to="/kapi/recipes/terminology-qa">
      <h3>Enforce terminology</h3>
      <p>Import a glossary and flag terminology drift in translated files before they ship.</p>
    </Link>
  </div>
  <div className="col col--6 margin-bottom--lg">
    <Link className="card padding--lg" to="/kapi/recipes/prepare-content">
      <h3>Prepare content for translation</h3>
      <p>Protect sensitive content, reuse past translations, and keep terminology and quality consistent — one repeatable pass before anything ships.</p>
    </Link>
  </div>
  <div className="col col--6 margin-bottom--lg">
    <Link className="card padding--lg" to="/kapi/recipes/choose-a-translation-provider">
      <h3>Choose a translation provider</h3>
      <p>Select an LLM or MT backend, wire its credentials safely, and make it the default — including on-device translation with Ollama.</p>
    </Link>
  </div>
  <div className="col col--6 margin-bottom--lg">
    <Link className="card padding--lg" to="/kapi/recipes/pre-translate-with-tm">
      <h3>Pre-translate with TM + termbase</h3>
      <p>Leverage existing TM, fill the gaps, and pre-flag terminology — deterministic, no API key, before any vendor sees content.</p>
    </Link>
  </div>
  <div className="col col--6 margin-bottom--lg">
    <Link className="card padding--lg" to="/kapi/bilingual-workflow">
      <h3>Run a bilingual round-trip</h3>
      <p>Extract to XLIFF, hand it to a translator, merge the targets back — with full TM provenance. Project-based.</p>
    </Link>
  </div>
  <div className="col col--6 margin-bottom--lg">
    <Link className="card padding--lg" to="/kapi/recipes/resumable-workspace">
      <h3>Work in a <code>.klz</code> workspace</h3>
      <p>A single-file, serverless workspace: extract sources in, run tools in place, merge the finished files out — config travels with the file.</p>
    </Link>
  </div>
  <div className="col col--6 margin-bottom--lg">
    <Link className="card padding--lg" to="/kapi/modes">
      <h3>Modes &amp; bindings</h3>
      <p>How ad-hoc and project runs differ, the source/sink binding model, and when it makes sense to mix them.</p>
    </Link>
  </div>
</div>

## Next steps

- [Installation](/kapi/get-started/installation) — Homebrew, WinGet, and binary downloads
- [Command reference](/commands) — every command and flag
- [Kapi Desktop](/kapi/desktop/overview) — the visual companion for building flows and running tools
- [Using kapi with AI assistants](/reference/mcp) — wire kapi into Claude Code, Cursor, and more
