---
id: estimate-cost
title: "Estimate a run's cost"
sidebar_label: "Estimate a job"
sidebar_position: 2
description: "Count blocks, words, and characters in any supported format to size a translation job before you commission it."
hide_table_of_contents: true
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import { StatsWidget } from "@site/src/components/Lab";

# Estimate a run's cost

**Goal:** know what a translation run will cost before you start it. kapi counts what there actually is to translate — blocks, words, characters, off the **extracted content**, so boilerplate, keys, and markup are excluded — and you multiply by a model's or vendor's per-word rate. Try it in your browser:

<StatsWidget />

<Tabs groupId="surface" queryString>
<TabItem value="desktop" label="Desktop" default>

Open the project — each content collection lists its matched files with per-file and collection totals, so the volume of a job is visible before you run anything. For a loose file, the word-count and stats tools run as **quick tools** from the Toolbox.

</TabItem>
<TabItem value="agent" label="Agent (Claude)">

Ask the sizing question directly:

> How big is translating `docs/` into French and Japanese — words, and a rough cost at $0.08/word?

The assistant runs `kapi stats --json` over the content, multiplies out the rate you gave, and can subtract what the TM already covers ([leverage first](/kapi/recipes/pre-translate-with-tm)) so the estimate reflects only the work that remains.

</TabItem>
<TabItem value="cli" label="CLI">

Point at a file, a directory, or a glob; totals roll up into one figure:

```bash
kapi word-count messages.json
kapi word-count ./locales --json
kapi stats --json docs/*.md          # richer: blocks, segments, by-role breakdown
```

In a project, run with no arguments to count the recipe's declared content. Flags: [`word-count`](/reference/commands/word-count); the field-by-field output is in the reference, not restated here.

</TabItem>
</Tabs>

## Next

- [Reuse what you've translated](/kapi/recipes/pre-translate-with-tm) — shrink the job before pricing it.
- [Hand off to translators](/kapi/bilingual-workflow) — extract, translate, merge back.
