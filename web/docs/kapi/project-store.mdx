---
sidebar_position: 2
title: The project store
description: Where a kapi project's work lives — one local database at .kapi/store.db, a free-to-delete cache, and the committed record of decisions, terms and memory. The recipe is config, the store is work, the files are the deliverable.
keywords: [kapi, project store, store.db, context graph, units, kapi commit, review, coverage, cache, working state, ship state]
---

# The project store

A kapi project has three things, and it helps to keep them straight:

- the **recipe** (`kapi.yaml`) — your *config*: languages, content, flows, defaults;
- the **files** — your *deliverable*: the source and target files on disk;
- the **project store** — your *work*: what kapi keeps under `.kapi/` while you
  build, plus the committed record of the decisions you make along the way.

This page is about the third one. The guiding rule:

> **Files and the committed context are the source of truth. `.kapi/store.db`
> is a derived index over them — plus the decisions you have staged but not yet
> committed.**

## What's in the store

| Layer | Lives in | Kind | Lose it? |
|---|---|---|---|
| **committed context** | `.kapi/units/*.jsonl`, plus the terms and memory bundles the recipe binds | **authored** — review decisions, approved terms, memory pairs | **committed — keep it in git** |
| local store | `.kapi/store.db` | **derived index** over the committed context and your content, **plus** the working set of staged decisions | rebuilds — except decisions staged since the last `kapi commit` |
| cache | `.kapi/cache/` | **derived** — parse cache, extractions, collections, sync cache | rebuilds on next run |

A project keeps **one** local database. `.kapi/store.db` holds every subsystem's
tables in a single SQLite file, on a single connection, each with its own
migration ledger: the block cache, the terms store, the content memory, the
unit-state working set, and the property graph (`graph_nodes` / `graph_edges`)
that links a term to the blocks that use it, a decision to the unit it blesses,
and both to their
[context coordinates](/contribute/implementation/kapi-project-file#context-coordinates).

It sits at the **top** of `.kapi/`, not under `cache/`, and the distinction is
the whole point: `rm -rf .kapi/cache` is completely free, while `store.db`
carries the working set. Between a review decision landing and `kapi commit`,
that working set holds the only copy of the staged decisions. Delete `store.db`
and you lose at most what you staged since the last commit; everything else is
rebuilt from the committed context and your files.

The committed side is plain text, and it is what a person reads in a pull
request:

- `.kapi/units/*.jsonl` — the unit record, one JSON Lines shard per document
  ([serialization](/reference/serialization/project-state));
- the terms bundle the recipe binds with `defaults.terms_source` (a
  `.terms.json`) and the content-memory bundle it binds with
  `defaults.memory_source` (a `.memory.json`);
- the content files themselves.

`.kapi/.gitignore` keeps `store.db` and `cache/` out of git; `.kapi/units/`
stays committed.

## One local context graph

Because the graph tables live beside the block, terms and memory tables, a
project answers graph questions without a server: which blocks use this term,
in which collection, at which coordinate. Local and connected projects converge
in shape — a Bowrain server answers the same query shapes over one Postgres
spanning workspaces, projects and streams, and a local project answers them
over `store.db` with those dimensions fixed to a single value. See
[AD-039: The Local Context Graph Store](/contribute/architecture/039-local-context-graph-store).

## Two stages: working, then ship

The store also explains the two stages of a run:

1. **Working** — `kapi run` (with no `-o`, in a project) lands its translations
   as overlays in the block tables of `store.db`. The work is *in the store, not
   yet in your files*.
2. **Ship** — `kapi merge` writes those overlays out to the **target files**;
   `kapi status` then reads the files (plus the unit record) for the real ship
   state — the [kapi loop](/kapi/convergence)'s ladders and gates.

So coverage has two honest readings: "what's translated in the store" (working,
pre-merge) and "what's in the files and reviewed" (ship). The Kapi desktop shows
both as one **Project status** surface you can toggle between; the CLI's
`kapi status` reports the ship reading.

## Decisions are staged, then committed

Approving a translation is a *decision*, and decisions are explicit, like a git
commit. When you approve a unit (see [Review](/kapi/convergence#review-a-decision-in-the-unit-record)),
the decision lands in the working set inside `store.db`. `kapi status` reports
what is pending, and `kapi commit` materializes the working set into
`.kapi/units/`, where you commit it with your sources. The decision is bound to
the **content hash** of the translation it blesses, so editing that translation
later drops the unit back below *reviewed* — an approval can't silently outlive
the text it approved.

```bash
kapi status --review        # the worklist: units awaiting approval
kapi apply <<<'{"kind":"review","file":"src/nb.json","id":"save.label","locale":"nb","status":"reviewed"}'
kapi status                 # staged decisions, not yet committed
kapi commit                 # → .kapi/units/*.jsonl, ready for git
```

The same model maps to a server-backed project: instead of committing shards,
it **pushes** its decisions to a remote (`kapi push`) and **pulls** them back
(`kapi pull`) — same decisions, different sink.

## An empty subsystem is an absent store

Presence is table-level. A `store.db` whose terms tables are empty behaves
exactly as though there were no terms store at all: on a fresh checkout the
terminology gate reads the committed `.terms.json` directly, `kapi up --plan`
never creates state, and `kapi pack` carries only the parts that hold
something. Nothing is conjured into existence by the file simply being there.

In the browser, the same model runs without SQLite: the memory and terms
backends are in-memory, blocks are keyed by path in memory, and the working set
persists to a JSON sidecar at `.kapi/store.json`.

## What the store is *not*

- It is **not** the content memory. The `.memory.json` bundle (`defaults.memory_source`) is a
  **recycle** corpus — `kapi apply` with `kind:"memory"` adds leverage for future
  translation; it does **not** record a review decision. Review state lives in the
  unit record. See [Memory & terms storage](/kapi/recipes/memory-and-terms-storage).
- It is **not** authoritative for your translations — the **files** are. The store
  indexes and stages; the deliverable is the source of truth.

## See also

- [The kapi loop](/kapi/convergence) — the derived ladder, gates, and review queue.
- [Projects](/kapi/projects) — how a run's source and sink resolve.
- [Project file reference](/reference/project-file) — `defaults.terms_source`,
  `defaults.memory_source`, and the other recipe fields.
