// GENERATED by scripts/walkthrough-gen from web/docs/walkthroughs/kapi-klz-workspace.scene.yaml.
// Do not edit by hand — change the .scene.yaml and regenerate.
import type { WalkthroughEmbedConfig } from "./types";

const config: WalkthroughEmbedConfig = {
  id: "kapi-klz-workspace",
  scene: "klz-workspace",
  mode: "interactive",
  seed: ["messages.json"],
  files: [],
  steps: [
    {
      command: "kapi extract messages.json -o work.klz --target-lang qps",
      narration:
        "extract reads the source into a portable .klz and records a recipe (locales + output layout).",
    },
    {
      command: "kapi pseudo-translate work.klz",
      narration:
        "The tool caches its per-block work in the workspace's shadow cache — the .klz file itself is not rewritten yet.",
    },
    {
      command: "kapi info work.klz",
      narration:
        'info reports the workspace is now "dirty" — there is work not yet packed into the .klz.',
    },
    {
      command: "kapi pack work.klz",
      narration:
        'pack writes the .klz — the explicit hand-off boundary. info would now show "clean".',
    },
    {
      command: "kapi merge work.klz -o out/",
      narration:
        "merge writes the translated documents from the workspace, one per source and locale.",
    },
  ],
};

export default config;
