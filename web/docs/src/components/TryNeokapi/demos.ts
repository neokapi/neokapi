// The three demo transforms the Try-Neokapi modal offers, all driven by the REAL
// kapi WASM engine (no faked mocks): a search/replace over the translatable
// text, content insights (block/word/char counts), and pseudo-translation.

export type DemoId = "search-replace" | "insights" | "pseudo";
