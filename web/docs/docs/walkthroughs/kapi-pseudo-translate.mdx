---
id: kapi-pseudo-translate
title: "Pseudo-translate to catch UI truncation early"
sidebar_label: "Pseudo-translate"
description: "Generate locale-shaped pseudo-translations with kapi to surface UI truncation bugs before real translation arrives."
---

import KapiGuidedEmbed from "@site/src/components/KapiPlayground/KapiGuidedEmbed";

# Pseudo-translate to catch UI truncation early

> Driven by [`walkthroughs/kapi-pseudo-translate.scene.yaml`](https://github.com/neokapi/neokapi/blob/main/web/docs/walkthroughs/kapi-pseudo-translate.scene.yaml) — one source generates both this interactive embed and the recorded video. Do not edit by hand; change the scene spec and regenerate.

You're shipping a feature with new strings and want to catch UI truncation problems _before_ the strings come back from a real translation provider. Pseudo-translation expands every string with locale-shaped accent characters so issues surface immediately on screen — no API key, no waiting, no cost.

## Try it

`messages.json` is in the in-browser project folder. Run the step, then open `messages.fr.json` in the files pane to see the expansion.

<KapiGuidedEmbed id="kapi-pseudo-translate" />

Pseudo-translation expands every string with diacritical characters so truncation, clipping, or missing-string bugs are immediately visible when the UI re-renders. It's part of the development loop, not the translation pipeline.

## Next

For deeper QA, see [Quality checks](/features/qa-checks). To run pseudo-translation in the same flow as a TM-leverage step, see [`kapi run`](/commands?id=run).
