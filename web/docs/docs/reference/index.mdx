---
id: index
slug: /reference
title: Reference
sidebar_label: Overview
sidebar_position: 0
description: Generated, authoritative reference for the neokapi framework — commands, format readers and writers, processing tools, flows, parity results, and benchmarks derived directly from the live codebase.
keywords: [neokapi reference, commands, formats, tools, flows, parity, benchmarks, generated reference]
---

# Reference

Generated, authoritative documentation for everything the framework exposes —
commands, formats, and tools — derived directly from the code and the format
and tool schemas.

## Interactive references

These pages are generated from the live registries and schemas:

- [Command Reference](/commands) — every `kapi` command, with runnable examples.
- [Format Reference](/formats) — every format reader/writer and its configurable
  parameters.
- [Tool Reference](/tools) — every processing tool and its inputs and outputs.
- [Flow Visualization](/flow-visualization) — built-in flows rendered as graphs.

## Project status

- [Parity](/parity) — coverage against the Okapi Framework.
- [Benchmarks](/pseudobench) — performance measurements.
- [Test Results](/test-comparison) — filter test comparison dashboard.

:::note

Every command, format, and tool also has its own static, indexable page under
`/reference/commands/<id>`, `/reference/formats/<id>`, and
`/reference/tools/<id>` — generated from the same data and linked from each card
in the grids above. These per-entry pages are the canonical, shareable URLs; the
grids remain the browse index.

:::
