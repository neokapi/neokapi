---
id: kapi-bilingual-workflow
title: "Round-trip translation with kapi extract → merge"
sidebar_label: "Bilingual workflow"
description: "The canonical extract → translate → merge round-trip via XLIFF. Per-locale outputs, translator fills targets, merge writes back with full TM provenance."
hide_table_of_contents: true
---

import KapiGuidedEmbed from "@site/src/components/KapiPlayground/KapiGuidedEmbed";
import { ThemedVideo } from "@neokapi/docs-shared";

# Round-trip translation with kapi extract → merge

> Driven by [`walkthroughs/kapi-bilingual-workflow.scene.yaml`](https://github.com/neokapi/neokapi/blob/main/web/docs/guides/kapi-bilingual-workflow.scene.yaml) — one source generates both the embed and the recorded video. Do not edit by hand; change the scene spec and regenerate.

The "extract → translate → merge" round-trip is the canonical bilingual workflow. Extract emits XLIFF per target locale (with TM pre-fill); the translator (or a vendor's tool) fills `<target>` elements; merge applies those back onto the source files and absorbs the new pairs into the TM with full provenance.

## The full round-trip

The complete round-trip — seeding the TM, extracting, a translator filling the targets, merging back, and `kapi tm audit` tracing the result — is shown in the recording below. The translator step is an external editor action and `tm audit` reads the SQLite TM, so the full loop runs against the native binary.

<ThemedVideo
  sources={{
    light: "/video/kapi/01-bilingual-workflow.webm",
    dark: "/video/kapi/01-bilingual-workflow.webm",
  }}
/>

A complete round-trip on a small fixture project: seed the TM from a corporate TMX, extract per-locale XLIFFs, simulate a translator filling them in, merge back, and use `kapi tm audit` to trace exactly what the merge wrote.

## Try the extract step

`kapi extract` runs entirely in the browser. A small recipe (`app.kapi`) and source catalogue are already in the in-browser project folder. Run the step, then open the emitted XLIFF under `out/` in the files pane.

<KapiGuidedEmbed id="kapi-bilingual-workflow" />

Extract emits one bilingual XLIFF per configured target locale, each carrying the kapi batch IDs that `kapi merge` later uses to write translations back onto the source.

## Next

`kapi tm audit --batch` is the receipt: every merge is provenance-tagged, so you can prove what came from which translator return.
