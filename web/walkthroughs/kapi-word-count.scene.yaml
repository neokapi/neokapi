# Unified scene spec (W3, issue #661). See kapi-overview.scene.yaml for the
# full schema. Regenerate with:
#   node --experimental-strip-types scripts/walkthrough-gen/gen.ts kapi-word-count

id: kapi-word-count
scene: word-count
mode: interactive

seed:
  - messages.json
files: []

steps:
  - comment: Size the content of a JSON file
  - command: kapi stats messages.json
    narration: Reports blocks, words, characters, and segments — the numbers you paste into a quote.
  - comment: Content stats help estimate translation costs
