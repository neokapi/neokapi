# Governing guidance
The support team now opens Monday–Saturday, 08:00–18:00 UTC. Use that exact display string in support_hours. The support email and message-handling policy are unchanged. Only support_hours needs editing. The archive records the previous hours and must remain unchanged; the API file describes a different service whose hours have not changed. Avoid unsupported guarantees.
