# Governing guidance
Only current product references in title and body change. Replace the exact product name Harbor Desk with Harbor Help. The quote is an attributed historical quotation; preserve it exactly. The api_id is a stable identifier. The archive and API file are excluded. Avoid unsupported guarantees.
