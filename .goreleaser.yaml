version: 2

project_name: kapi

before:
  hooks:
    # Create placeholder so bowrain/apps/bowrain/main.go's //go:embed all:frontend/dist succeeds
    # without a real frontend build (bowrain is built separately in the release workflow).
    - mkdir -p bowrain/apps/bowrain/frontend/dist && touch bowrain/apps/bowrain/frontend/dist/placeholder
    # Tests are already run by the release workflow before GoReleaser.
    - go mod tidy
    - bash -c "cd cli && go mod tidy"
    - bash -c "cd platform && go mod tidy"
    - bash -c "cd kapi && go mod tidy"
    - bash -c "cd bowrain-cli && go mod tidy"
    - bash -c "cd bowrain && go mod tidy"

builds:
  - id: kapi
    dir: kapi
    main: ./cmd/kapi
    binary: kapi
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/gokapi/gokapi/core/version.Version={{.Version}}
      - -X github.com/gokapi/gokapi/core/version.Commit={{.Commit}}
      - -X github.com/gokapi/gokapi/core/version.BuildDate={{.Date}}

  - id: bowrain-cli
    dir: bowrain-cli
    main: ./cmd/bowrain
    binary: bowrain
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/gokapi/gokapi/core/version.Version={{.Version}}
      - -X github.com/gokapi/gokapi/core/version.Commit={{.Commit}}
      - -X github.com/gokapi/gokapi/core/version.BuildDate={{.Date}}

archives:
  - id: kapi
    ids:
      - kapi
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]

  - id: bowrain-cli
    ids:
      - bowrain-cli
    name_template: "bowrain-cli_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]

brews:
  - name: kapi
    ids:
      - kapi
    repository:
      owner: gokapi
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/gokapi/gokapi"
    description: "AI-native localization framework — format-aware parsing, concurrent pipelines, and pluggable tools"
    license: "Apache-2.0"
    download_strategy: GitHubPrivateRepositoryReleaseDownloadStrategy
    custom_require: "lib/private_download_strategy"
    test: |
      system "#{bin}/kapi", "version"

  - name: bowrain-cli
    ids:
      - bowrain-cli
    repository:
      owner: gokapi
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/gokapi/gokapi"
    description: "Bowrain CLI — manage localization projects, sync with Bowrain Server"
    license: "Apache-2.0"
    download_strategy: GitHubPrivateRepositoryReleaseDownloadStrategy
    custom_require: "lib/private_download_strategy"
    extra_install: |
      bin.install_symlink bin/"bowrain" => "brain"
    test: |
      system "#{bin}/bowrain", "version"
      system "#{bin}/brain", "version"

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  abbrev: 7
  format: "[`{{ .SHA }}`](https://github.com/gokapi/gokapi/commit/{{ .SHA }}) {{ .Message }}"
  groups:
    - title: Features
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: Bug Fixes
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: Others
      order: 999
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  mode: append
  header: |
    ## Installation

    **Homebrew (kapi CLI):**
    ```
    brew install gokapi/tap/kapi
    ```

    **Homebrew (bowrain CLI):**
    ```
    brew install gokapi/tap/bowrain-cli
    ```

    **Homebrew (Bowrain desktop app, macOS):**
    ```
    brew install --cask gokapi/tap/bowrain
    ```

    **Docker (server):**
    ```
    docker pull ghcr.io/gokapi/bowrain-server:{{ .Version }}
    ```

    ## Platform Support

    | Component | Linux amd64 | Linux arm64 | macOS (universal) | Windows amd64 | Windows arm64 |
    |-----------|:-----------:|:-----------:|:------------------:|:-------------:|:-------------:|
    | kapi CLI | ✓ | ✓ | ✓ | ✓ | ✓ |
    | bowrain CLI | ✓ | ✓ | ✓ | ✓ | ✓ |
    | Bowrain Desktop | ✓ | ✓ | ✓ | ✓ | ✓ |
    | Docker (server) | ✓ | ✓ | — | — | — |
