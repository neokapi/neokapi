// Expo Router requires an index route. The RootLayout in _layout.tsx
// renders the full app navigator, so this component is unused.
// Returning null keeps the router satisfied.
export default function Index() {
  return null;
}
