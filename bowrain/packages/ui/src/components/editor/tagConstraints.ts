// Re-export from shared package — implementation lives in @neokapi/ui-primitives.
export { resolveConstraints, isDeletable, isCloneable } from "@neokapi/ui-primitives";
export type { ResolvedConstraints } from "@neokapi/ui-primitives";
