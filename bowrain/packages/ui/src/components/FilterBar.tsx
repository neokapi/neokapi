/**
 * Re-export FilterBar from the shared UI primitives package.
 * The canonical implementation lives in @neokapi/ui-primitives.
 */
export {
  FilterBar,
  type FilterBarProps,
  type FilterToken,
  type FilterField,
  type FilterPreset,
} from "@neokapi/ui-primitives";
