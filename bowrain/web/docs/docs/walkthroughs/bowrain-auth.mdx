---
id: bowrain-auth
title: "Sign into Bowrain Server from the CLI"
sidebar_label: "Auth"
description: "kapi auth — login, logout, and status against Bowrain Server."
---

import { ThemedVideo } from "@neokapi/docs-shared";

# Sign into Bowrain Server from the CLI

> Generated from [`walkthroughs/bowrain-auth.md`](https://github.com/neokapi/neokapi/blob/main/bowrain/web/docs/walkthroughs/bowrain-auth.md). Do not edit by hand — change the prompt and regenerate.

`kapi auth` is how you sign into Bowrain Server from the CLI. Quick status check; help for the subcommands.

## Scene 1 — auth

<ThemedVideo
  sources={{
    light: "/video/bowrain-cli/01-auth.webm",
    dark: "/video/bowrain-cli/01-auth.webm",
  }}
/>

`kapi auth --help` lists login/logout/status. `kapi auth status` prints the active session (or that you're signed out).

## Next

For the full device-OAuth flow, see [`kapi auth login`](/cli/commands/auth).
