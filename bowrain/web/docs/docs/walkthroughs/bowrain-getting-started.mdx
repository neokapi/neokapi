---
id: bowrain-getting-started
title: "Getting started from a local codebase (kapi)"
sidebar_label: "Developer path (kapi)"
description: "Connect a local project to Bowrain with kapi and sync translations like git: init, push, status, pull."
---

import { ThemedVideo } from "@neokapi/docs-shared";

# Getting started from a local codebase (kapi)

The kapi developer path: connect a local project to your Bowrain server, push the
source for the team to translate, and pull translations back — synced like git.
This is the on-ramp for a developer working from a local codebase; if your
content lives in a CMS, a design tool, or a git host,
[connect it server-side](/server/connectors) instead — no checkout needed.

<ThemedVideo
  sources={{
    light: "/video/bowrain-cli/bowrain-cli-getting-started-light.webm",
    dark: "/video/bowrain-cli/bowrain-cli-getting-started-dark.webm",
  }}
/>

`kapi init --server https://app.bowrain.cloud` writes a `<dir-name>.kapi` recipe and a sibling
`.kapi/` state directory and connects the project to your Bowrain server (pass
`--workspace <slug>` if you belong to several). `kapi add` tracks the files you
want translated; `kapi push` sends the source up (pure transport); `kapi status`
shows standing, exactly like git; and `kapi pull` brings translations back into
the same files and formats. `kapi up` runs the whole kapi loop on the
server — push, catch up, and pull in one command, for the daily reconcile and CI.

## Next

- [Auth and workspaces](/walkthroughs/bowrain-auth) — sign in and choose where to push.
- [Automation](/walkthroughs/bowrain-automation) — local rules + GitHub Actions.
- Command reference: [init](/cli/commands/init), [push](/cli/commands/push),
  [pull](/cli/commands/pull), [up](/cli/commands/up), [status](/cli/commands/status).
