# Built binaries (in platform/ root from local dev builds)
/bowrain-server
/bowrain-worker

# Local stack secrets (compose.full.yaml reads bowrain/.env)
/.env

# Local-dev TLS certs (mkcert; regenerate with `make -C bowrain certs`)
docker/traefik/certs/*.pem
