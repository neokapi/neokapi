# Webpack + React + @neokapi/kapi-react

```bash
npm install
npm run dev              # Dev mode — source text
npx kapi-react extract      # Extract strings
LOCALE=de npm run build  # Build with German translations
```
