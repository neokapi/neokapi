# Suppress peer dependency warnings from @storybook/react-vite's transitive
# dependency on @joshwooding/vite-plugin-react-docgen-typescript, which hasn't
# declared support for Vite 8 (bundled by vite-plus) yet. Safe to ignore —
# Storybook 10.3 works fine with Vite 8.
loglevel=error
