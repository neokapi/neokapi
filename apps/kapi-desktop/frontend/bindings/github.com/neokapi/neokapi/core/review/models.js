// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Create as $Create } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as check$0 from "../check/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as model$0 from "../model/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as profile$0 from "../profile/models.js";
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as state$0 from "../state/models.js";

/**
 * Context binds one review decision to the context it is made in.
 */
export class Context {
    /**
     * Creates a new Context instance.
     * @param {Partial<Context>} [$$source = {}] - The source object to create the Context.
     */
    constructor($$source = {}) {
        if (!("point" in $$source)) {
            /**
             * Point is where the content sits and what governs it there.
             * @member
             * @type {Point}
             */
            this["point"] = (new Point());
        }
        if (!("neighbourhood" in $$source)) {
            /**
             * Neighbourhood is the unit's key and the blocks around it in document
             * order.
             * @member
             * @type {Neighbourhood}
             */
            this["neighbourhood"] = (new Neighbourhood());
        }
        if (!("history" in $$source)) {
            /**
             * History is what this unit said before, and the wording the content memory
             * already holds for it.
             * @member
             * @type {History}
             */
            this["history"] = (new History());
        }
        if (!("judgement" in $$source)) {
            /**
             * Judgement is what the checks and the AI pre-review found.
             * @member
             * @type {Judgement}
             */
            this["judgement"] = (new Judgement());
        }
        if (!("provenance" in $$source)) {
            /**
             * Provenance is where the current target came from and who decided on it.
             * @member
             * @type {Provenance}
             */
            this["provenance"] = (new Provenance());
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new Context instance from a string or object.
     * @param {any} [$$source = {}]
     * @returns {Context}
     */
    static createFrom($$source = {}) {
        const $$createField0_0 = $$createType0;
        const $$createField1_0 = $$createType1;
        const $$createField2_0 = $$createType2;
        const $$createField3_0 = $$createType3;
        const $$createField4_0 = $$createType4;
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        if ("point" in $$parsedSource) {
            $$parsedSource["point"] = $$createField0_0($$parsedSource["point"]);
        }
        if ("neighbourhood" in $$parsedSource) {
            $$parsedSource["neighbourhood"] = $$createField1_0($$parsedSource["neighbourhood"]);
        }
        if ("history" in $$parsedSource) {
            $$parsedSource["history"] = $$createField2_0($$parsedSource["history"]);
        }
        if ("judgement" in $$parsedSource) {
            $$parsedSource["judgement"] = $$createField3_0($$parsedSource["judgement"]);
        }
        if ("provenance" in $$parsedSource) {
            $$parsedSource["provenance"] = $$createField4_0($$parsedSource["provenance"]);
        }
        return new Context(/** @type {Partial<Context>} */($$parsedSource));
    }
}

/**
 * History is what has already been approved for this unit.
 */
export class History {
    /**
     * Creates a new History instance.
     * @param {Partial<History>} [$$source = {}] - The source object to create the History.
     */
    constructor($$source = {}) {
        if (/** @type {any} */(false)) {
            /**
             * Prior is the last answer approved for this block, with the source it was
             * approved for. Either half alone is worse than neither: a target with no
             * source is an anchor with no explanation.
             * @member
             * @type {PriorVersion | null | undefined}
             */
            this["prior"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * Match is the content memory's best answer for this source, with its
             * wording. A percentage alone tells a reviewer that something close exists
             * and never what it says.
             * @member
             * @type {MemoryMatch | null | undefined}
             */
            this["match"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * Unseeded reports a project whose committed context sources have never
             * been compiled into the store this history reads: a fresh clone, before
             * anything ran. The store answers, and answers empty, which a reviewer
             * cannot tell from a memory that genuinely holds nothing close. `kapi up`
             * compiles the sources; until it has, an empty Match means unread.
             * @member
             * @type {boolean | undefined}
             */
            this["unseeded"] = undefined;
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new History instance from a string or object.
     * @param {any} [$$source = {}]
     * @returns {History}
     */
    static createFrom($$source = {}) {
        const $$createField0_0 = $$createType6;
        const $$createField1_0 = $$createType8;
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        if ("prior" in $$parsedSource) {
            $$parsedSource["prior"] = $$createField0_0($$parsedSource["prior"]);
        }
        if ("match" in $$parsedSource) {
            $$parsedSource["match"] = $$createField1_0($$parsedSource["match"]);
        }
        return new History(/** @type {Partial<History>} */($$parsedSource));
    }
}

/**
 * Judgement is what has already been said about this translation.
 */
export class Judgement {
    /**
     * Creates a new Judgement instance.
     * @param {Partial<Judgement>} [$$source = {}] - The source object to create the Judgement.
     */
    constructor($$source = {}) {
        if (/** @type {any} */(false)) {
            /**
             * Findings are the checkers' results for this unit, each with the run range
             * it applies to, so a surface can point at the text rather than describe
             * it.
             * @member
             * @type {check$0.Finding[] | undefined}
             */
            this["findings"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * AIScore, AIModel and AIFindings are the fresh AI pre-review annotation
             * from the state store, where the host keeps one.
             * @member
             * @type {number | null | undefined}
             */
            this["ai_score"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * @member
             * @type {string | undefined}
             */
            this["ai_model"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * @member
             * @type {state$0.AIReviewFinding[] | undefined}
             */
            this["ai_findings"] = undefined;
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new Judgement instance from a string or object.
     * @param {any} [$$source = {}]
     * @returns {Judgement}
     */
    static createFrom($$source = {}) {
        const $$createField0_0 = $$createType10;
        const $$createField3_0 = $$createType12;
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        if ("findings" in $$parsedSource) {
            $$parsedSource["findings"] = $$createField0_0($$parsedSource["findings"]);
        }
        if ("ai_findings" in $$parsedSource) {
            $$parsedSource["ai_findings"] = $$createField3_0($$parsedSource["ai_findings"]);
        }
        return new Judgement(/** @type {Partial<Judgement>} */($$parsedSource));
    }
}

/**
 * MemoryMatch is the content memory's best answer for this unit's source.
 */
export class MemoryMatch {
    /**
     * Creates a new MemoryMatch instance.
     * @param {Partial<MemoryMatch>} [$$source = {}] - The source object to create the MemoryMatch.
     */
    constructor($$source = {}) {
        if (!("score" in $$source)) {
            /**
             * Score is the match percent (0-100).
             * @member
             * @type {number}
             */
            this["score"] = 0;
        }
        if (/** @type {any} */(false)) {
            /**
             * Kind is how the memory matched it: "exact", "fuzzy", or one of the
             * generalized and structural variants the memory reports.
             * @member
             * @type {string | undefined}
             */
            this["kind"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * Source is the wording the corpus holds for the source locale, and Target
             * the answer approved for it.
             * @member
             * @type {string | undefined}
             */
            this["source"] = undefined;
        }
        if (!("target" in $$source)) {
            /**
             * @member
             * @type {string}
             */
            this["target"] = "";
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new MemoryMatch instance from a string or object.
     * @param {any} [$$source = {}]
     * @returns {MemoryMatch}
     */
    static createFrom($$source = {}) {
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        return new MemoryMatch(/** @type {Partial<MemoryMatch>} */($$parsedSource));
    }
}

/**
 * Neighbour is one neighbouring block.
 */
export class Neighbour {
    /**
     * Creates a new Neighbour instance.
     * @param {Partial<Neighbour>} [$$source = {}] - The source object to create the Neighbour.
     */
    constructor($$source = {}) {
        if (/** @type {any} */(false)) {
            /**
             * Key is the neighbour's stable unit key, so a reviewer can address it.
             * @member
             * @type {string | undefined}
             */
            this["key"] = undefined;
        }
        if (!("source" in $$source)) {
            /**
             * Source is the neighbour's source content. The prompt carries this half.
             * @member
             * @type {model$0.Run[]}
             */
            this["source"] = [];
        }
        if (/** @type {any} */(false)) {
            /**
             * Target is what the locale under review says for the neighbour, absent
             * when nothing is translated there. The prompt never carries it; a
             * reviewer reading a paragraph in sequence does.
             * @member
             * @type {model$0.Run[] | undefined}
             */
            this["target"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * Status is the neighbour's rung on the target ladder in this locale,
             * empty when it has no target.
             * @member
             * @type {string | undefined}
             */
            this["status"] = undefined;
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new Neighbour instance from a string or object.
     * @param {any} [$$source = {}]
     * @returns {Neighbour}
     */
    static createFrom($$source = {}) {
        const $$createField1_0 = $$createType13;
        const $$createField2_0 = $$createType13;
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        if ("source" in $$parsedSource) {
            $$parsedSource["source"] = $$createField1_0($$parsedSource["source"]);
        }
        if ("target" in $$parsedSource) {
            $$parsedSource["target"] = $$createField2_0($$parsedSource["target"]);
        }
        return new Neighbour(/** @type {Partial<Neighbour>} */($$parsedSource));
    }
}

/**
 * Neighbourhood is the unit in its document: its key, and the blocks either
 * side of it in the order the file holds them.
 * 
 * The blocks travel as Run sequences rather than as text. Flattening a run
 * sequence into a string reads as "concatenate the text" and behaves as
 * "delete every placeholder, every paired code, every plural", so a reader
 * gets a sentence with the variables silently missing from it.
 */
export class Neighbourhood {
    /**
     * Creates a new Neighbourhood instance.
     * @param {Partial<Neighbourhood>} [$$source = {}] - The source object to create the Neighbourhood.
     */
    constructor($$source = {}) {
        if (/** @type {any} */(false)) {
            /**
             * Key is the block's key or path: `app.settings.title` rather than `Save`.
             * The cheapest disambiguation signal there is, and the one the prompt
             * always carries.
             * @member
             * @type {string | undefined}
             */
            this["key"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * Before and After are the neighbouring blocks, nearest last in Before and
             * nearest first in After, so reading the three lists in order reads the
             * document in order.
             * @member
             * @type {Neighbour[] | undefined}
             */
            this["before"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * @member
             * @type {Neighbour[] | undefined}
             */
            this["after"] = undefined;
        }
        if (!("window" in $$source)) {
            /**
             * Window is how many blocks either side were asked for. A list shorter
             * than the window means the document ended, rather than that blocks were
             * dropped.
             * @member
             * @type {number}
             */
            this["window"] = 0;
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new Neighbourhood instance from a string or object.
     * @param {any} [$$source = {}]
     * @returns {Neighbourhood}
     */
    static createFrom($$source = {}) {
        const $$createField1_0 = $$createType15;
        const $$createField2_0 = $$createType15;
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        if ("before" in $$parsedSource) {
            $$parsedSource["before"] = $$createField1_0($$parsedSource["before"]);
        }
        if ("after" in $$parsedSource) {
            $$parsedSource["after"] = $$createField2_0($$parsedSource["after"]);
        }
        return new Neighbourhood(/** @type {Partial<Neighbourhood>} */($$parsedSource));
    }
}

/**
 * Point is the coordinate the unit's file sits at, with the governance in force
 * there: the same answer `kapi context <path>` and the `context://` resources
 * serve, so a reviewer and a run are never told different things about one
 * file.
 */
export class Point {
    /**
     * Creates a new Point instance.
     * @param {Partial<Point>} [$$source = {}] - The source object to create the Point.
     */
    constructor($$source = {}) {
        if (/** @type {any} */(false)) {
            /**
             * Path is the SOURCE file, project-relative and slash-separated. On the
             * platform it is the item the block belongs to.
             * @member
             * @type {string | undefined}
             */
            this["path"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * Language is the language the unit under review belongs to, which is what
             * the governance below was resolved for: a term rule is resolved per
             * language, so a point read for one language answers for that one.
             * @member
             * @type {string | undefined}
             */
            this["language"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * IsSource marks the language as the project's source language, so a client
             * knows it is looking at the author's own wording rather than a translation
             * of it.
             * @member
             * @type {boolean | undefined}
             */
            this["is_source"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * Profile is the governance profile in force, empty at the project's
             * default point.
             * @member
             * @type {string | undefined}
             */
            this["profile"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * Channel is the surface the content ships on, empty when it binds none.
             * @member
             * @type {string | undefined}
             */
            this["channel"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * Collection is the content collection claiming the path.
             * @member
             * @type {string | undefined}
             */
            this["collection"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * Ref renders Profile and Channel as the recipe writes the binding
             * (`profile/channel`).
             * @member
             * @type {string | undefined}
             */
            this["ref"] = undefined;
        }
        if (!("default" in $$source)) {
            /**
             * Default reports that resolution fell through to the project's default
             * point, which is a real place rather than an absent one.
             * @member
             * @type {boolean}
             */
            this["default"] = false;
        }
        if (/** @type {any} */(false)) {
            /**
             * Coordinates place the point on the declared axes: product, channel, and
             * the brand a recipe states once under `defaults:`.
             * @member
             * @type {{ [_ in string]?: string } | undefined}
             */
            this["coordinates"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * Voice is the profile in force with the guidance it renders: the same
             * prose `kapi voice guide` prints and the translate prompt carries.
             * @member
             * @type {Voice | null | undefined}
             */
            this["voice"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * TermRules are the constraints on wording in force here, in the shape
             * every governed tool takes them (`term_rules:`). The rules bearing on this
             * unit's own wording lead the list.
             * @member
             * @type {profile$0.TermRule[] | undefined}
             */
            this["term_rules"] = undefined;
        }
        if (!("terms_total" in $$source)) {
            /**
             * TermsTotal is how many rules the point binds in all, so a capped list
             * says what it is a part of.
             * @member
             * @type {number}
             */
            this["terms_total"] = 0;
        }
        if (/** @type {any} */(false)) {
            /**
             * Profiles are the recipe's bounded governance profiles read against now:
             * which voice is in force, and until when.
             * @member
             * @type {ProfileValidity[] | undefined}
             */
            this["profiles"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * Notes carries the freshness and scope caveats the resolution produced, so
             * a thin point is never ambiguous between "nothing governs here" and
             * "nothing could be read".
             * @member
             * @type {string[] | undefined}
             */
            this["notes"] = undefined;
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new Point instance from a string or object.
     * @param {any} [$$source = {}]
     * @returns {Point}
     */
    static createFrom($$source = {}) {
        const $$createField8_0 = $$createType16;
        const $$createField9_0 = $$createType18;
        const $$createField10_0 = $$createType20;
        const $$createField12_0 = $$createType22;
        const $$createField13_0 = $$createType23;
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        if ("coordinates" in $$parsedSource) {
            $$parsedSource["coordinates"] = $$createField8_0($$parsedSource["coordinates"]);
        }
        if ("voice" in $$parsedSource) {
            $$parsedSource["voice"] = $$createField9_0($$parsedSource["voice"]);
        }
        if ("term_rules" in $$parsedSource) {
            $$parsedSource["term_rules"] = $$createField10_0($$parsedSource["term_rules"]);
        }
        if ("profiles" in $$parsedSource) {
            $$parsedSource["profiles"] = $$createField12_0($$parsedSource["profiles"]);
        }
        if ("notes" in $$parsedSource) {
            $$parsedSource["notes"] = $$createField13_0($$parsedSource["notes"]);
        }
        return new Point(/** @type {Partial<Point>} */($$parsedSource));
    }
}

/**
 * PriorVersion is one block's previous source and the target approved for it,
 * read from the content memory's version chain.
 */
export class PriorVersion {
    /**
     * Creates a new PriorVersion instance.
     * @param {Partial<PriorVersion>} [$$source = {}] - The source object to create the PriorVersion.
     */
    constructor($$source = {}) {
        if (!("source" in $$source)) {
            /**
             * @member
             * @type {string}
             */
            this["source"] = "";
        }
        if (!("target" in $$source)) {
            /**
             * @member
             * @type {string}
             */
            this["target"] = "";
        }
        if (/** @type {any} */(false)) {
            /**
             * ContextFingerprint is the governing context that answer was produced
             * under. A translate prompt withholds a prior version whose fingerprint no
             * longer matches; a reviewer is shown it together with what it was
             * approved under, because judging whether the rules moved is the
             * reviewer's job.
             * @member
             * @type {string | undefined}
             */
            this["context_fingerprint"] = undefined;
        }
        if (!("governed" in $$source)) {
            /**
             * Governed reports that the fingerprint still matches the context the
             * decision was recorded under, which is the condition under which the
             * prompt would have carried this pair.
             * @member
             * @type {boolean}
             */
            this["governed"] = false;
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new PriorVersion instance from a string or object.
     * @param {any} [$$source = {}]
     * @returns {PriorVersion}
     */
    static createFrom($$source = {}) {
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        return new PriorVersion(/** @type {Partial<PriorVersion>} */($$parsedSource));
    }
}

/**
 * ProfileValidity is a governance profile whose validity is bounded, reported
 * so an answer can say which voice is in force and until when.
 */
export class ProfileValidity {
    /**
     * Creates a new ProfileValidity instance.
     * @param {Partial<ProfileValidity>} [$$source = {}] - The source object to create the ProfileValidity.
     */
    constructor($$source = {}) {
        if (!("name" in $$source)) {
            /**
             * @member
             * @type {string}
             */
            this["name"] = "";
        }
        if (/** @type {any} */(false)) {
            /**
             * ValidFrom and ValidTo render the profile's window as it was authored: a
             * bare date when it is midnight, otherwise the full instant. Empty means
             * the bound is open on that side.
             * @member
             * @type {string | undefined}
             */
            this["valid_from"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * @member
             * @type {string | undefined}
             */
            this["valid_to"] = undefined;
        }
        if (!("state" in $$source)) {
            /**
             * State is the window read against now: "active", "upcoming" (not yet in
             * force) or "expired".
             * @member
             * @type {string}
             */
            this["state"] = "";
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new ProfileValidity instance from a string or object.
     * @param {any} [$$source = {}]
     * @returns {ProfileValidity}
     */
    static createFrom($$source = {}) {
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        return new ProfileValidity(/** @type {Partial<ProfileValidity>} */($$parsedSource));
    }
}

/**
 * Provenance is where the current target came from and who decided on it.
 */
export class Provenance {
    /**
     * Creates a new Provenance instance.
     * @param {Partial<Provenance>} [$$source = {}] - The source object to create the Provenance.
     */
    constructor($$source = {}) {
        if (/** @type {any} */(false)) {
            /**
             * Origin is the target's provenance when the state store or the format
             * records one.
             * @member
             * @type {model$0.Origin | null | undefined}
             */
            this["origin"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * ReviewState, By, At and Note are the decision in force. There is one per
             * (scope, unit, variant) and a new decision overwrites it, so this is the
             * current decision rather than the most recent of several.
             * @member
             * @type {string | undefined}
             */
            this["review_state"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * Status is the ladder rung the decision in force landed the unit on: a
             * target rung for a translation, an authoring rung for source wording.
             * @member
             * @type {string | undefined}
             */
            this["status"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * @member
             * @type {string | undefined}
             */
            this["by"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * @member
             * @type {string | undefined}
             */
            this["at"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * @member
             * @type {string | undefined}
             */
            this["note"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * Stale reports a decision recorded against source wording that has since
             * changed: the reviewer blessed a rendering of a sentence the project no
             * longer has.
             * @member
             * @type {boolean | undefined}
             */
            this["stale"] = undefined;
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new Provenance instance from a string or object.
     * @param {any} [$$source = {}]
     * @returns {Provenance}
     */
    static createFrom($$source = {}) {
        const $$createField0_0 = $$createType25;
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        if ("origin" in $$parsedSource) {
            $$parsedSource["origin"] = $$createField0_0($$parsedSource["origin"]);
        }
        return new Provenance(/** @type {Partial<Provenance>} */($$parsedSource));
    }
}

/**
 * Voice is the voice in force at a point, with the guidance it renders. It is
 * the same row the by-location context answer carries, because it is the same
 * fact.
 */
export class Voice {
    /**
     * Creates a new Voice instance.
     * @param {Partial<Voice>} [$$source = {}] - The source object to create the Voice.
     */
    constructor($$source = {}) {
        if (!("name" in $$source)) {
            /**
             * @member
             * @type {string}
             */
            this["name"] = "";
        }
        if (/** @type {any} */(false)) {
            /**
             * Source is where the profile was loaded from: a path, `pack:<name>`, or
             * `store:<id>`.
             * @member
             * @type {string | undefined}
             */
            this["source"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * Field names the recipe key that bound it (`profiles.<name>.voice` or
             * `defaults.voice`), so a caller that wants to change the answer knows
             * which line to edit. Empty where no recipe bound it.
             * @member
             * @type {string | undefined}
             */
            this["field"] = undefined;
        }
        if (/** @type {any} */(false)) {
            /**
             * Guide is the profile rendered as prose for a model: the same rendering
             * `kapi voice guide` prints and the translation prompt carries, so what a
             * reviewer reads here is what generation was held to.
             * @member
             * @type {string | undefined}
             */
            this["guide"] = undefined;
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new Voice instance from a string or object.
     * @param {any} [$$source = {}]
     * @returns {Voice}
     */
    static createFrom($$source = {}) {
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        return new Voice(/** @type {Partial<Voice>} */($$parsedSource));
    }
}

// Private type creation functions
const $$createType0 = Point.createFrom;
const $$createType1 = Neighbourhood.createFrom;
const $$createType2 = History.createFrom;
const $$createType3 = Judgement.createFrom;
const $$createType4 = Provenance.createFrom;
const $$createType5 = PriorVersion.createFrom;
const $$createType6 = $Create.Nullable($$createType5);
const $$createType7 = MemoryMatch.createFrom;
const $$createType8 = $Create.Nullable($$createType7);
const $$createType9 = check$0.Finding.createFrom;
const $$createType10 = $Create.Array($$createType9);
const $$createType11 = state$0.AIReviewFinding.createFrom;
const $$createType12 = $Create.Array($$createType11);
const $$createType13 = $Create.Array($Create.Any);
const $$createType14 = Neighbour.createFrom;
const $$createType15 = $Create.Array($$createType14);
const $$createType16 = $Create.Map($Create.Any, $Create.Any);
const $$createType17 = Voice.createFrom;
const $$createType18 = $Create.Nullable($$createType17);
const $$createType19 = profile$0.TermRule.createFrom;
const $$createType20 = $Create.Array($$createType19);
const $$createType21 = ProfileValidity.createFrom;
const $$createType22 = $Create.Array($$createType21);
const $$createType23 = $Create.Array($Create.Any);
const $$createType24 = model$0.Origin.createFrom;
const $$createType25 = $Create.Nullable($$createType24);
