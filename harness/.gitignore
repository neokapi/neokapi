# Secrets — never commit
.env
.env.*
!.env.example

# Generated capture + render artifacts (regenerable from demos/ + capture pipeline)
captures/
out/
public/
node_modules/
.remotion/
*.log

# Per-demo sandbox working copies (regenerated from demos/<id>/fixtures)
sandbox/

# Isolated kapi state (own plugins/home, populated by `npm run setup`)
.kapi/
