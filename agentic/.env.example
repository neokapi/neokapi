# Azure OpenAI — for Developer, PM, QA agents (GPT-4o-mini / GPT-4o)
AZURE_OPENAI_API_KEY=

# Azure AI Foundry — for Translator, Brand Manager agents (Claude Sonnet)
AZURE_AI_FOUNDRY_KEY=

# GitHub — for gh CLI (filing issues against neokapi/agent-feedback)
# Tip: use `gh auth token` to get your current token
GITHUB_TOKEN=

# Per-agent Bowrain JWT tokens (created via Keycloak user setup or Bravo conversation API)
ALEX_AGENT_TOKEN=
JEANPIERRE_AGENT_TOKEN=
MARIA_AGENT_TOKEN=
KATRIN_AGENT_TOKEN=
YUKI_AGENT_TOKEN=
LISA_AGENT_TOKEN=
TAYLOR_AGENT_TOKEN=
