# {{PROJECT_NAME}} Localization Experiment
name: {{PROJECT_NAME}}
slug: {{PROJECT_SLUG}}-l10n
description: "AI agents localizing {{PROJECT_NAME}} — {{PROJECT_DESCRIPTION}}"

upstream:
  repo: {{UPSTREAM_ORG}}/{{UPSTREAM_REPO}}
  fork: neokapi/agentic-{{PROJECT_SLUG}}

source_language: en-US
target_languages:
  - fr-FR
  - de-DE
  # Add more target languages as needed

content_paths:
  - path: "{{CONTENT_PATH}}"
    format: {{CONTENT_FORMAT}}

# Agents for this workspace (referenced by directory name)
agents:
  - name: {{ENGINEER_NAME}}
    role: "L10N Engineer"
  # Add language experts and reviewers below
